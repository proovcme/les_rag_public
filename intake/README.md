# intake — каталог приёма файлов оператора

Вход для незакрытых карточек LES3. Реальные файлы кладёт сюда оператор; система их
потребляет. Сами файлы НЕ коммитятся (`.gitignore`), READMEs — коммитятся.

| Папка | Вопрос | Карточка-потребитель |
|---|---|---|
| `xsd_minstroy/`      | Q4  | W19.1/W19.3 (реестр форм, рендер+валидация XML), W14 |
| `document_samples/`  | Q5  | W11.3 (forms_service template-fill), W20.1 (сметы), W20.2 (спецификации) |
| `normcontrol_stamp/` | Q6  | W13.1 v2 (layout-анализ штампа) |
| `bim_models/`        | Q11 | W17.3 (хребет), W6.1 (агрегатные чанки), W9 (норм↔элемент) |
