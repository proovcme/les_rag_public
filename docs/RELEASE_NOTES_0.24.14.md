# ЛЕС 0.24.14

- В «Документах» можно выбрать до 20 файлов и открыть чат только по ним.
- Закреплены служебные датасеты «Нормативы и чек-листы», «Прайсы» и «Сметные источники»; файлы добавляются штатной кнопкой, сметный чат подключает прайсы автоматически.
- Настройка длины ответа теперь действительно управляет объёмом генерации; стандартный режим сохраняет хорошо отвечающий baseline Legion.
- По явной просьбе модель может посмотреть реальную страницу или область PDF глазами, с указанием исходного файла и страницы.
- Индикаторы RAG и сметной базы разделяют рабочую механическую базу и необязательный поисковый ускоритель, без ложного сообщения «индекс отсутствует».
