# Diff Retirement Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remove the unintegrated W12.1 diff product island while preserving the active CAD/BIM viewer, import inventory and document evidence workflows.

**Architecture:** Static consumer analysis shows that `/api/diff/*` is registered but has no UI, model tool, dossier or interservice consumer. The active `/api/cad-bim/imports` already owns import inventory; retirement removes only the parallel diff router, isolated service and service-only tests without changing CAD/BIM graph data or documents.

**Tech Stack:** Python 3.12, FastAPI, pytest, uv.

**Spec:** `ROADMAP_TO_V1.md`; `docs/MODULE_INDEX.md`; `docs/CODE_MAP.md`; historical context `docs/LES3_PLAN.md` W12.1/W12.2.

## Global Constraints

- Do not modify `frontend/cad_bim_viewer/**`, `proxy/smeta_core/**`, RAG, Qdrant or installed runtime state.
- Do not delete or migrate CAD/BIM graph data, imports, datasets, documents or generated artifacts.
- Preserve active `/api/cad-bim/imports`, `/api/cad-bim/*` viewer routes and document evidence behavior.
- Work in the existing cleanup branch; do not create another branch or worktree.
- Use a failing route/module inventory test before deleting production files.

---

### Task 1: Retire the isolated W12.1 contour

**Files:**
- Modify: `tests/test_code_runtime_map.py`
- Modify: `proxy/app.py`
- Delete: `proxy/routers/diff.py`
- Delete: `proxy/services/diff_service.py`
- Delete: `tests/test_diff_service.py`

**Interfaces:**
- Consumes: generated module and FastAPI route inventory.
- Produces: no `/api/diff/*` routes or executable diff modules; `/api/cad-bim/imports` and the CAD/BIM viewer router remain registered.

- [x] **Step 1: Add the failing boundary test**

Add `test_unintegrated_diff_is_retired_without_removing_cad_bim_inventory` to `tests/test_code_runtime_map.py`. Assert that `/api/cad-bim/imports` remains, both diff module paths are absent and every `/api/diff` route is absent.

- [x] **Step 2: Verify RED**

Run: `uv run pytest tests/test_code_runtime_map.py -q --basetemp=.test-tmp/diff-retirement-red`

Expected: FAIL because both diff modules and three routes still exist.

- [x] **Step 3: Remove the isolated contour**

Delete the router import and registration from `proxy/app.py`, then delete the router, service and service-only test. Do not touch CAD/BIM SQLite, viewer code, document storage or RAG.

- [x] **Step 4: Regenerate inventory and verify GREEN**

Run: `uv run python tools/code_runtime_map.py`

Run: `uv run pytest tests/test_code_runtime_map.py -q --basetemp=.test-tmp/diff-retirement-green`

Expected: PASS; registered routes decrease by three while `/api/cad-bim/imports` remains.

### Task 2: Synchronize release documentation

**Files:**
- Modify: `docs/MODULE_INDEX.md`
- Modify: `docs/CODE_MAP.md`
- Modify: `docs/LES3_PLAN.md`
- Modify: `config/version.json`
- Modify: `docs/RELEASE_LEDGER.md`
- Generated by version sync: version-bearing manifests and lock files

**Interfaces:**
- Consumes: final runtime-map counts from Task 1.
- Produces: version `0.30.17`, build `657`, desktop `5.1.657`, with explicit records that CAD/BIM data and active import inventory were preserved.

- [x] **Step 1: Update current maps and historical ledger**

Record the removed W12.1 island, retained CAD/BIM inventory/viewer behavior, exact inventory counts and the non-destructive data boundary. Mark W12.1 as retired historical implementation without erasing why it existed.

- [x] **Step 2: Synchronize version files**

Update `config/version.json`, then run: `make version-sync`

- [x] **Step 3: Run canonical gates**

Run: `make architecture-gate`

Run: `make verify`

Run: `make test`

Expected: all commands exit zero with no failed tests.

- [x] **Step 4: Commit locally**

Commit all files with `chore: retire unintegrated diff island`; do not push or deploy.
