# Worklog Retirement Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remove the unfinished W20.3 worklog product island while preserving the active field journal, project records and all user data.

**Architecture:** Static consumer analysis shows that `/api/worklog/*` is registered but has no UI, model tool, project dossier or interservice consumer. Retirement removes the router, isolated service and service-only tests; it does not alter `les_work_log_meta`, field journal rows, generated files or the OЖР glossary concept.

**Tech Stack:** Python 3.12, FastAPI, pytest, uv.

**Spec:** `ROADMAP_TO_V1.md`; `docs/MODULE_INDEX.md`; `docs/CODE_MAP.md`; historical context `docs/LES3_PLAN.md` W20.3/W20.5.

## Global Constraints

- Do not modify `proxy/smeta_core/**`, RAG, Qdrant or installed runtime state.
- Do not delete or migrate `les_work_log_meta`, field-journal rows, generated files or any user database.
- Preserve active `/api/field/*`, project services and the OЖР glossary/retrieval behavior.
- Work in the existing cleanup branch; do not create another branch or worktree.
- Use a failing route/module inventory test before deleting production files.

---

### Task 1: Retire the isolated W20.3 contour

**Files:**
- Modify: `tests/test_code_runtime_map.py`
- Modify: `proxy/app.py`
- Delete: `proxy/routers/worklog.py`
- Delete: `proxy/services/work_log_service.py`
- Delete: `tests/test_work_log_service_w203.py`

**Interfaces:**
- Consumes: generated module and FastAPI route inventory.
- Produces: no `/api/worklog/*` routes or executable worklog modules; `/api/field/*` remains registered and OЖР glossary code remains tracked.

- [x] **Step 1: Add the failing boundary test**

Add `test_unfinished_worklog_is_retired_without_removing_field_or_glossary` to `tests/test_code_runtime_map.py`. Assert that `/api/field` remains, both worklog module paths and every `/api/worklog` route are absent, and `proxy/services/glossary_chat_service.py` remains.

- [x] **Step 2: Verify RED**

Run: `uv run pytest tests/test_code_runtime_map.py -q --basetemp=.test-tmp/worklog-retirement-red`

Expected: FAIL because both worklog modules and four routes still exist.

- [x] **Step 3: Remove the isolated contour**

Delete the router import and registration from `proxy/app.py`, then delete the router, service and service-only test. Do not touch SQLite, generated worklog files, field data or glossary code.

- [x] **Step 4: Regenerate inventory and verify GREEN**

Run: `uv run python tools/code_runtime_map.py`

Run: `uv run pytest tests/test_code_runtime_map.py -q --basetemp=.test-tmp/worklog-retirement-green`

Expected: PASS; registered routes decrease by four while `/api/field/*` remains.

### Task 2: Synchronize release documentation

**Files:**
- Modify: `docs/MODULE_INDEX.md`
- Modify: `docs/CODE_MAP.md`
- Modify: `docs/LES3_PLAN.md`
- Modify: `config/version.json`
- Modify: `docs/RELEASE_LEDGER.md`
- Generated by version sync: version-bearing manifests and lock files

**Interfaces:**
- Consumes: final runtime-map counts from Task 1.
- Produces: version `0.30.16`, build `656`, desktop `5.1.656`, with an explicit record that persistent state and OЖР knowledge were preserved.

- [x] **Step 1: Update current maps and historical ledger**

Record the removed W20.3 island, retained field/glossary behavior, exact inventory counts and the non-destructive data boundary. Mark W20.3 as retired historical implementation without erasing why it existed.

- [x] **Step 2: Synchronize version files**

Update `config/version.json`, then run: `make version-sync`

- [x] **Step 3: Run canonical gates**

Run: `make architecture-gate`

Run: `make verify`

Run: `make test`

Expected: all commands exit zero with no failed tests.

- [x] **Step 4: Commit locally**

Commit all files with `chore: retire unfinished worklog island`; do not push or deploy.
