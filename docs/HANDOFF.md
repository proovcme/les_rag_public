# HANDOFF — Л.Е.С. (состояние и бэклог на 2026-06-18)

Снимок после большой сессии «ИИ-СОД». Что работает, что НЕ сделано, и как продолжить.
Ветка: `feat/les3-p1` (запушена в public `proovcme/les_rag`, pre-release `v0.1.1-dev`).
Детали решений — `docs/ADR-12-typed-retrieval.md`, ADR-таблица в `docs/LES3_PLAN.md`.

## Что живёт и проверено

- **Позиционирование:** «Единый центр информации о строительном проекте» — ИИ-СОД + агент РП/ГИП (README).
- **Детерминированная агрегация по сметам/ВОР:** SQL по полному Parquet, не LLM. Кабель 3х1,5 = 15 030,72 м (бит-в-бит). `proxy/services/table_query_service.py`.
- **.xls читаются:** xlrd + конвертация в `parquet_writer`; дренаж индексатора; `RAG_PARSE_MIN_FREE_GB=4` под 9B.
- **Типизированный ретрив (ADR-12, флаг `LES_TYPED_RETRIEVAL=true`):** LLM-роутер по каталогу + кэш `doc_router_cache`. `proxy/services/doc_router.py`.
- **Индексация внешней папки по ссылке:** `POST /api/rag/index-external` + `LES_EXTERNAL_SOURCE_ROOTS`. ИД ЭОМ → 87 файлов, 0 копий.
- **Облако:** proxyapi.ru (Cloudflare/OpenRouter из РФ режутся). **Локаль:** Qwen3.5-9B-MLX-4bit.

## Сессия 2026-06-18 — сделано (код в репо, `make verify` зелёный, 5 коммитов)

Все пункты бэклога **кроме АРТЕЛИ** закрыты на уровне кода (детерминированные ядра, 0 LLM где обещано; 43 новых офлайн-теста). Осталось деплой/данные/мерж — см. «остатки» ниже.

1. ✅ **Сверка ВОР↔КС-2↔смета↔ИД** — `proxy/services/reconcile_service.py` (qty-приоритет + data-aware fallback по полям объёма, кластеризация позиций по ед.+наименованию, флаги match/mismatch/gap/single, допуск 1%). Роутер `/api/bor/reconcile` (preview/generate/download). 16 тестов. *Остаток: проиндексировать реальные КС-2/ЛС + распарсить ИД (87 файлов, parse=False) — это данные, не код.*
2. ✅ **GUI-вид сверки + Сводный ВОР** — карта «СВЕРКА» на странице Инструменты (`sovushka/pages/instrumenty.py`): мультивыбор датасетов, динамические колонки по источникам, цветной статус. ВОР-таблица уже была. *Остаток: живой скриншот (нужен порт в рантайм + рестарт sovushka + данные).*
3. ✅ **Мультикласс через диалог** — `proxy/services/class_router_service.py` (детектор классов норматив/смета/письмо/проект + suggestions). Чипы «Посмотреть как …» в чате (стрим+нестрим). ADR-12: «через диалог, не авто-fan-out». 10 тестов.
4. ⏸ **АРТЕЛЬ — MEP-коннекторы** (`task_91f89b7d`) — НЕ трогали по просьбе; туда применить полученные знания (W10.3 three-layer, контракты).
5. ✅ **Формирование ответа по интенту** — `proxy/services/answer_form_service.py` (value/enum/full/brief/default → каркас + потолок токенов, аддитивно к sys_normal). 9 тестов.
6. ✅ **OCR сканов → gemma4:12b** — `OllamaVisualOCRParser` + фабрика `make_ocr_parser` (RAG_OCR_BACKEND=ollama|mlx), converter переключён, MLX-путь сохранён. 8 тестов.

## Сессия 2026-06-18 (продолжение) — деплой + сверка через чат

- ✅ **Деплой в рантайм** `/Users/ovc/LES`: все коммиты портированы, proxy+sovushka рестартнуты (mlx/qdrant не трогали — 9B тёплая). Поймали и починили `UnboundLocalError` (`retrieval_trace`) уже на живом (`1741371`).
- ✅ **Сверка как задача чата** (`reconcile_chat_service.py`, W11.4b, коммит `9a5c8b0`): «сверь/сходятся/соответствие/расхождения» → канал `reconcile` (до clarification), система сама находит Parquet-датасеты, сверяет, отвечает ГИП-стилем. Живо: route=reconcile, VERIFIED, 0 LLM.
- ✅ **gemma4:12b** уже подтянут в ollama (7.6 ГБ, на :11434), runtime `.env` имеет `OLLAMA_BASE_URL`; OCR-код дефолтит на него. Directive #3 закрыта на уровне инфры.
- ⏳ **ИД распарсены частично: 17/87** (`M74_ID_EOM_Index`), 0 ошибок, без OCR (у этих ИД есть текстовый слой). Дали Parquet → reconcile теперь видит **2 типа: Ведомость + Смета(из ИД)**, 96 позиций (все `gap` — позиции ВОР и ИД не совпадают по наименованиям). Остановлено на ~1.7 ГБ свободной RAM (guard). Остаток 70 файлов — догнать при свободной RAM (parse-scheduler с гардами или ночью; 9B ест RAM).
- ⚠️ **В корпусе НЕТ настоящих КС-2/ЛС(смета):** только ВОР (1901+2101), ИД (сканы+доки) и акты. Но **акты/ведомости оборудования в ИД = рабочий 2-й источник.**
- ✅ **Игра с ведомостями+актами (коммит `540233d`):** два косяка вскрылись и починены —
  (1) `parquet_writer._map_columns_simple`: «Наименование оборудования/**материал**ов» ловилось `amount_mat` раньше `name` → имя терялось, строки отбрасывались (демонтажная ведомость: 1 мусор вместо 23 поз.). Фикс: «наименование»→name высоким приоритетом. Теперь 23 поз. (Розетка/Выключатель/Кабель/Бра…).
  (2) `reconcile` ось `by="dataset"` — сверять ДОКУМЕНТЫ (ведомость↔акт), не схлопывать по типу; чат по умолчанию `by="dataset"`+имена датасетов.
  **Вживую на рантайме нашло реальное расхождение:** «Коробка установочная 2 мод 50мм Batibox» — ВОР 395 шт vs Акт/ИД 68 шт (Δ 82.8%). ИД parquet перегенерирован фиксированным парсером (RAM-safe, без OCR).
- ✅ **Полировка интерфейса (коммит `b670d68`):** фильтр шума (заголовки секций «1. Бюро…» + числовой мусор «2» убраны из сверки), чистые ярлыки источников (`TABLE_SMETA_Index`→`TABLE SMETA`, `_pretty_label`), endpoint `/api/bor/reconcile?by=dataset` (default) + резолв имён из метабазы → GUI-карта СВЕРКА и xlsx сравнивают ДОКУМЕНТЫ как чат. Живо: 114→**108** позиций, расхождение Batibox 395 vs 68 сохранено.
- **Остаётся (по желанию):** fuzzy-match наименований по артикулу/типоразмеру (080051) — чтобы сверка ловила совпадения, когда наименования формулированы по-разному (ВОР «Кабель медный ППГнг 3х1,5» vs ИД «Кабель 3х1,5 ВВГнг» — это РАЗНЫЕ кабели, текущая сверка верно даёт gap; fuzzy нужен для одинаковых позиций с разным написанием).

- ✅ **Скрепка чата (W11.8, коммит за `b670d68`):** кнопка-скрепка в композере → диалог (режим + загрузка). `POST /api/rag/attach`: `mode=quick` — парс таблиц в Parquet-вложение без векторов (сверка обнаруживает автоматически, ярлык = имя файла `_name.txt`, авто-очистка >6ч); `mode=index` — полная индексация в датасет «Вложения чата». `sovushka.api_post_file` (multipart→proxy). Живо: прикрепил демонтажную ведомость → стала 3-м источником сверки, 9 расхождений. 0 LLM на парсе. Триггеры сверки в чате расширены (оборудование/монтаж/материалы/вложение).

- ✅ **Браузер папок (W11.9):** «пути руками писать неудобно» → `GET /api/rag/browse-external` (навигация по корням `LES_EXTERNAL_SOURCE_ROOTS` кликами, счёт файлов, «вверх», та же защита `validate_external_source`). В Самоваре кнопка «Обзор…» у поля внешней папки → диалог-браузер → «Выбрать эту папку». Живо проверено. *Паттерн переиспользуемый — если есть другие поля с ручным путём, подключить туда же.*

- ✅ **Спецификация (Ф9) → ВОР работ (W11.10):** алгоритм `docs/ALGO-spec-to-bor.md` (0 LLM): позиция спецификации → строка работы, объём = кол-во, глагол по словарю категорий (кабель→Прокладка, оборудование→Монтаж, крепёж/розетка→Установка; порядок правил защищает «кабельный лоток»→Прокладка). `spec_to_bor_service` + `/api/bor/{id}/from-spec` + GUI-переключатель «Свод / Работы из спец.(Ф9)» + чат «сделай ВОР из спецификации». Живо: TABLE_SMETA → 93 работы из 213 позиций.

- ✅ **Тон + память диалога (W11.11):** `sys_normal` усилен (едко-ироничный, изящное хамство — числа строгие). `memory_service.session_memory` подмешивает реплики текущей сессии в промпт LLM (чат был потурно безсостоятельным → теперь «помнит всё» в рамках диалога; + прежний кросс-сессионный `recall_context`). Живо: 2-й вопрос вспомнил 1-й (VERIFIED), тон заметно ироничнее. Предпочтение в памяти агента `sovushka-tone-and-memory`.

- ✅ **Авто-заметки (W11.12):** `memory_service.maybe_autonote` — утверждение-факт (не вопрос/команда + маркер связки « — »/«это»/«называется»/«телефон»/«срок»…) ЛЕС сохраняет сам (`auto=1`) и подтверждает. Гарды строгие (запрос без «?» без маркера не ловится). `LES_AUTONOTE_ENABLED` (default true). Живо: «Прораб — Иван Петров» → авто-заметка #3; «требования к серверным сп 485» не попал. 0 LLM.

## Остатки (мерж)

- **#7 Влить `feat/les3-p1` → main** + настоящий релиз — когда контур стабилизируется (полный `make test` на живых сервисах + доменный golden 16/16). Придержано по решению Олега 2026-06-18 («стабилизировать»). 7 коммитов на ветке, локально, НЕ запушено.

## Известный долг / наблюдать

- **Валидатор Т.О.С.К.А. (coreml NLI):** точность golden ~25%, fail-open (ответ виден, штамп VERIFIED не доверять). Тюнить пороги/режимы.
- **Локальный 9B:** ~73с/ответ на M4 (холодный старт + декод). Облако (proxyapi) — быстрый primary; 9B — приватный fallback.
- **Naming-долг АРТЕЛЬ:** `Agnostis`/`MyVeras` → `ARTEL` (ренейм после стабилизации контрактов).
- **Whisper (голосовые заметки):** модель оставлена, фича не сделана (W8.2).
- **table_query поле:** работает на кабеле; на новых датасетах проверять выбор qty/amount (qty-приоритет + data-aware fallback).

## Как продолжить

- Рантайм: `/Users/ovc/LES` (live, launchd: mlx/proxy/qdrant/sovushka). Репо: `/Users/ovc/Projects/LES_v2`.
- Правки рантайма → порт в репо → `make verify` → commit. Гейт: `make verify` (офлайн).
- Память агента: `MEMORY.md` + `les3-rag-intake-state` (бэклог), `cloudflare-blocked-use-openai-direct`, `mlx-model-choice-m4`, `legion-build-workflow`, `artel-*`.
- Тесты сессии: `tests/test_table_query_service.py`, `tests/test_external_index.py` (15/15 зелёные).
