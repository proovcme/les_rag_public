# TODO · Windows-выпуск

Последнее обновление: 2026-07-14. Завершённая история выпусков хранится в Git и
[`RELEASE_LEDGER.md`](RELEASE_LEDGER.md); здесь остаются только открытые действия.

## Перед выпуском 0.24.1 / сборка 410

- [ ] Свести выпускную ветку в `main` и удалить только доказанно слитые временные ветки.
- [x] Выполнить `make patch-release` из чистого отправленного commit.
- [x] Подтвердить изолированную установку реального EXE, terminal `ready`, `/api/version`,
  Qdrant `dense + bm25_sparse`, RRF и интерфейс.
- [x] Опубликовать `latest.json`, `LES-Setup.exe`, `LES-Setup.exe.sha256`; скачать их обратно и
  сверить версию и SHA-256.
- [ ] Проверить обновление предыдущего выпуска кнопками «Проверить» → «Обновить».

## После выпуска

- [ ] Подтвердить общий реранкер отдельным live-запросом с двумя и более кандидатами; single-chunk
  release-smoke доказывает RRF, но по корректной оптимизации не вызывает reranker.
- [ ] Выполнить внешний пользовательский путь: вложение → идемпотентный сметный чат → XLSX →
  повтор того же ключа без второго вызова модели.
- [ ] Проверить parent/context expansion отдельным живым запросом после окончания фоновых работ.
- [ ] Добавить Authenticode, когда будет выбран сертификат; до этого SmartScreen остаётся
  документированным ограничением.

Каноническая процедура — [`INSTALL_RUNBOOK.md`](INSTALL_RUNBOOK.md), версии обязательного ПО —
[`SOFTWARE_VERSIONS.md`](SOFTWARE_VERSIONS.md).
