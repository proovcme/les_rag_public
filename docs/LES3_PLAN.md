# LES3_PLAN — план работ ЛЕС 3 (формат для агентов)

**Обновлён:** 2026-06-10 · **Статус:** утверждённый черновик
**База:** аудиты ЛЕС 2 (RAG-ядро, CAD/BIM, АРТЕЛЬ, UI, LLM-контур) + спека PDF-препроцессора (2026-06-07) + ревизия технологий RAG (2026-06-10).

---

## Как пользоваться этим планом (агенту)

1. Перед любой задачей прочитай [AGENTS.md](../AGENTS.md) и [docs/CODE_MAP.md](CODE_MAP.md). Guardrails оттуда действуют всегда и важнее этого плана.
2. Бери **одну карточку задачи** (`W*.N`). В карточке: файлы, что сделать, критерий приёмки. Не расширяй скоуп.
3. Цикл: минимальный дифф → точечный тест → **`make verify`** → доменный гейт, если карточка помечена `[gate:domain]` (`uv run python tools/rag_golden_set.py --cases golden/domain_fire_hvac_set.json`, база 16/16).
4. Выполнив задачу — поменяй `[ ]` на `[x]` в этом файле и допиши однострочный итог (дата, коммит).
5. **Без явного одобрения оператора нельзя:** полный реиндекс (`[reindex]`-задачи), рестарты сервисов, `uv add` (`[dep]`-задачи), удаление данных, изменение дефолтов рантайма (`VALIDATOR_BACKEND` и т.п.).
6. Застрял или карточка противоречит коду — **остановись и доложи**, не угадывай.

Метки: `S/M/L` — сложность · `[reindex]` — нужно окно переиндексации · `[dep]` — новая зависимость · `[gate:domain]` — обязателен доменный гейт · `[live]` — нужны живые Qdrant/MLX для проверки.

---

## Цели версии

1. **Стабильность** — индексация любого объёма без таймаутов и присмотра; падение стадии не теряет работу.
2. **Качество** — эмбеддер видит весь текст чанка (без молчаливых обрезок); ответы на структурные вопросы (пункты ГОСТ, этажи/системы BIM, сканы).
3. **Мультиплатформа** — Win/Mac первого класса, Linux через docker.
4. **Провайдер-агностик** — MLX, Ollama, llama.cpp, LM Studio, любой OpenAI-совместимый эндпоинт.
5. **Установка одной командой.**

---

## Зафиксированные архитектурные решения (ADR)

| # | Решение | Обоснование |
|---|---|---|
| ADR-1 | Эволюция, не переписывание: proxy/Qdrant/SQLite-метабаза/шеллы остаются | Аудит показал зрелое ядро; узкие места — в слоях |
| ADR-2 | Протокол OpenAI API — шина для всех LLM-провайдеров | mlx_host уже говорит `/v1/*`; Ollama/llama.cpp/LM Studio совместимы |
| ADR-3 | **Реранкер — cross-encoder модель, НЕ LLM-промпт.** Существующий `backend/reranker.py` (Qwen3-4B, «оцени 0-10») заменяется, не дорабатывается | Ревизия 2026-06: LLM-pointwise реранк устарел; cross-encoder быстрее на порядок, без парсинга, без конкуренции за Metal |
| ADR-4 | Лексическая ветка — learned sparse (BGE-M3 sparse / SPLADE) в Qdrant; самописный стеммер не развиваем | BGE-M3 уже в профилях, используется только dense-выход; Qdrant нативно держит sparse |
| ADR-5 | Конвертация PDF — layout-aware парсер (Docling или MinerU), pymupdf4llm остаётся fallback | Кросс-страничные таблицы спецификаций — текущая боль |
| ADR-6 | Сканы — отдельный визуальный индекс (ColPali/ColQwen, мультивекторы Qdrant), OCR для сканов не основной путь | Консенсус 2026: на сканах визуальный ретрив бьёт OCR-пайплайн |
| ADR-7 | Чанк-размеры в **токенах**, жёсткая проверка ≤ seq_len эмбеддера при старте | Сейчас qwen-профиль 1550 символов > 512 токенов — хвосты молча теряются |
| ADR-8 | Отвергнуто/отложено: полный GraphRAG (дорог; LightRAG — только эксперимент W6.6), LLM-контекстуализация чанков (есть механическая версия), семантический чанкер по эмбеддингам (structure-aware под ГОСТ лучше) | Не тащить хайп без выигрыша на наших golden sets |
| ADR-9 | **Оркестр облако+локал**: маршрутизация задач между локальными и облачными моделями по матрице «класс задачи × чувствительность данных × доступность». **Облако — только OpenRouter и OpenAI API** (решение оператора, 2026-06-10); прямых интеграций других вендоров не делаем. Оба говорят на OpenAI-протоколе → облако ездит по той же шине ADR-2, что и локальные провайдеры — отдельного SDK не нужно. OpenRouter — гибкость (десятки моделей через один эндпоинт, A/B без кода), OpenAI напрямую — Batch API (−50%) и промпт-кэш. Чувствительность — флаг на датасете: `P0 local-only` (почта, договоры, персональные данные) / `P1 cloud-ok` (нормативка, открытые справочники) / `P2 cloud-ok-с-согласия` (чертежи и данные проектов). Облако недоступно → деградация на локальный fallback, не отказ | Локальные модели закрывают 80% потока дёшево и приватно; облако даёт качество там, где локальные физически не тянут (рукопись, генерация семейств, сложный анализ чертежей); один протокол — меньше кода |
| ADR-10 | **Рукописные пометки — vision-VLM облачного класса + human-in-the-loop**: распознавание рукописи на чертежах локальными моделями ненадёжно; первичный путь — облачная vision-модель (GPT-5.5 через OpenAI/OpenRouter) со structured output, каждое извлечённое значение подтверждается человеком до записи в журнал объёмов. Локальная Gemma 4 12B (мультимодальная) — ступень предклассификации кропов до облака | Цена ошибки в учёте объёмов — деньги; рукопись — признанно сложный кейс даже для облачных VLM |
| ADR-11 | **LLM-минимализм**: LLM — последний инструмент, не первый. Порядок выбора для любой подзадачи: (1) детерминированный алгоритм/SQL/regex → (2) словарь/справочник/кэш → (3) специализированная малая модель (cross-encoder, эмбеддер, классификатор) → (4) локальная LLM → (5) облачная LLM. Каждый LLM-вызов в коде должен отвечать на вопрос «почему это нельзя сделать алгоритмом» — комментарием или ссылкой на ADR. Числа, суммы, агрегации LLM **никогда не считает** — только форматирует посчитанное. Роль LLM в системе: **понимание, объяснение, мост между человеком и системой**. Канон: статья оператора [«Границы применения LLM»](https://ai.ovc.me/llm-boundaries) — 7 запретов (UI, расчёты, хранение, XML, проверка норм, визуальное сравнение, инженерные решения) и целевая архитектура «алгоритм находит факты — LLM переводит на человеческий» | Скорость, детерминизм, тестируемость, цена. Лучшие части ЛЕС 2 (clause lookup, rules-валидатор, KOT-роутер) — уже алгоритмы; это направление, а не исключение |

**Что НЕ устарело и НЕ трогать без причины:** Qdrant; structure-aware сплиттер (ГОСТ-пункты); детерминированный clause lookup; C-RAG валидация как концепция; semantic cache; golden-гейты; логика памяти `backend/mlx_adapter.py`.

**Watch-list (наблюдаем, не делаем):** [apple/coreai-models](https://github.com/apple/coreai-models) — Core AI (`.aimodel`, требует **macOS 27+**; прод-Mac на 26 — блокер). Единственная интересная точка входа: замена Core ML-эмбеддера (наша боль — fixed-shape .mlpackage, батч 1, процесс-изоляция; в каталоге есть Qwen3). Критерий пересмотра: обновление прод-Mac до macOS 27 + динамические батчи в Core AI. Бонус-кандидаты на потом: SAM/CLIP для детекции зон пометок (W8.2), Whisper для голосовых заметок. LLM-инференс не трогаем — MLX остаётся (ADR-1).

---

## Рекомендации по моделям (срез июнь 2026, пересматривать ежеквартально)

### Локальный ярус (Mac / MLX; на Windows — те же через Ollama)

| Роль | Модель | Замечания |
|---|---|---|
| main (генерация) | **Qwen3.6-35B-A3B** (MoE, 3.5B активных) | Кандидат на замену текущей Qwen3-14B: быстрее на токен при выше качестве; 262K контекста. На 64GB+ RAM — вариант Qwen3.5-122B-A10B. Принимать через golden set, не по бенчмаркам |
| validator | Qwen3-4B (текущая) или 8B-класс | Каскад rules→LLM (W3.3) важнее размера модели |
| embedder | Qwen3-Embedding-0.6B (текущая) | Не менять без явной причины — реиндекс дорог; проверить более свежие только вместе с окном реиндекса W2 |
| reranker | Qwen3-Reranker-0.6B / bge-reranker-v2-m3 | См. W2.2 (ADR-3) |
| vision/мультимодал локальный | **Gemma 4 12B** (Ollama `gemma4:12b`; установлена 2026-06-11, Ollama 0.30.7) | Мультимодальная (текст+изображение+аудио), reasoning, 7.6 ГБ. **Полевой вывод 2026-06-11:** на прод-Mac (24 ГБ) рядом с MLX-стеком выталкивает систему в swap (86% → admission блокирует чат) и непрактично медленна на RAG-контекстах интерактивно. Роли: **только офлайн-окна** — предклассификация фото/кропов (W8.2), пакетные задачи при выгруженном MLX. Интерактивный чат — нет. Восстановление после: `ollama stop gemma4:12b` + рестарт MLX + дождаться дренажа swap |
| OCR локальный | GLM-OCR (Mac) — сравнить с Gemma 4 12B | Только печатный текст; рукопись — в облако (ADR-10) |

### Облачный ярус (только OpenRouter + OpenAI — ADR-9; цены за 1M токенов вход/выход)

| Роль | Модель | Цена | Когда |
|---|---|---|---|
| Тяжёлый анализ, vision чертежей/рукописи, генерация семейств | **GPT-5.5** (OpenAI или через OpenRouter) | $5/$30 | Задачи, где локальные не тянут; reasoning + vision + structured outputs; контекст 1M. **Внимание: >272K входных токенов — наценка 2×/1.5× на всю сессию, в т.ч. в Batch** — длинные документы резать до порога |
| Рабочая лошадка облачных задач | GPT-5.4 | $2.50/$15 | Массовая классификация пометок, нормоконтроль-проверки — лучший баланс цена/качество |
| Дешёвая классификация/экстракция | GPT-4.1 Nano / mini-класс через OpenRouter | от $0.10 вход | Маршрутизация, простая экстракция полей |
| A/B и страховка от вендор-лока | любая модель через OpenRouter | по прайсу модели | Один эндпоинт — сравнение моделей на наших golden-наборах без изменения кода |

Снижение стоимости: **OpenAI Batch API −50%** на всё неинтерактивное (ночные пакеты фото, массовый нормоконтроль) — только при прямом OpenAI, у OpenRouter батчей нет; **промпт-кэш OpenAI −75-90%** на повторных префиксах (системные промпты, справочник ФОП у генератора семейств) — кэш автоматический, но требует стабильного префикса промпта. Structured outputs — обязательны для всех экстракционных задач: убирают парсинг свободного текста.

### Матрица маршрутизации (политика диспетчера, W3.5)

| Класс задачи | Данные P0 | Данные P1/P2 |
|---|---|---|
| Чат-RAG, валидация, реранк | локально | локально (облако не нужно) |
| Query rewriting / multi-hop | локально | локально, облако опционально |
| OCR печатного текста | локально | локально |
| Рукопись, штампы, пометки | **запрещено / ручной ввод** | предкласс Gemma 4 локально → облако (GPT-5.5 vision) |
| Генерация семейств, сложный анализ | локально (деградация качества) | облако (GPT-5.5) |
| Массовые офлайн-пакеты | локально | OpenAI Batch −50% |

---

## Волна 0 — Измеримость (делается первой, всё остальное принимается через неё)

- [x] **W0.1 Пофазные метрики латентности** · S — 2026-06-10. Фазы `retrieval/context/generation/validation/overhead` в `chat_metrics.latency_phases` + лог `[METRICS] phases=...`; в `/api/metrics.pipeline` — последние 10 и средние (`summarize_phases`, runtime.py). Тесты tests/test_metrics_phases.py. Фаза rerank появится с новым реранкером в W2.2 (внутри retrieval-сервиса, не трогали — без доменного гейта).

- [x] **W0.2 CI офлайн-гейт** · S — 2026-06-10, `.github/workflows/verify.yml` (uv sync --frozen + make verify, macos-latest; локально гейт зелёный — 455 тестов собрано). Матрица Win — W4.3.

- [x] **W0.3 Расширить golden sets** · M — 2026-06-10 (кейсы); `[live]`-baseline ждёт окна. Три набора: `golden/clause_subclauses_set.json` (пункты с подпунктами — приёмка W2.6), `golden/bim_questions_set.json` (этажи/категории/агрегаты — приёмка W6.1), `golden/artel_fop_set.json` (ФОП/GUID/learning cases — приёмка W6.3). Базовый 16/16 не тронут; лоадер проверен.
  **Остаток:** один прогон на живом рантайме (`uv run python tools/rag_golden_set.py --cases golden/<set>.json`) — зафиксировать baseline-результаты в этом файле.

## Волна 1 — Стабильность индексации

- [ ] **W1.1 Конфиг-твики без реиндекса** · S — **частично 2026-06-10**: целевые значения и предупреждения внесены в env.example. ВАЖНО: `COREML_EMBED_BATCH_SIZE` зашит в fixed-shape .mlpackage (`b1_s512`) — поднятие батча = регенерация артефакта `tools/coreml_embedding_probe.py` (b4_s512), не правка числа. Остаток задачи: замер на живом рантайме (окно с оператором): `RAG_EMBED_BATCH` 16→64, `RAG_UPSERT_BATCH` 100→256, `MLX_EMBED_TTL_SEC` 300→1800, по одному с контролем RAM.
  Приёмка остатка: время индексации тестового датасета −30% или откат параметра.

- [x] **W1.2 Верификация точек выборочно** · S — 2026-06-10. Exact-count в Qdrant — каждый N-й файл и последний (`RAG_VERIFY_POINTS_EVERY=10`), вместо каждого ([qdrant_adapter.py](../backend/qdrant_adapter.py)). **Решение:** батч-UPDATE статусов НЕ делаем — по-файловая запись нужна resume-семантике W1.4; цена ~мс на файл, реальной экономией был count.

- [ ] **W1.3 PDF-препроцессор** · M
  Файлы: новые `tools/pdf_preprocess.py`, `tests/test_pdf_preprocess.py`; интеграция в [tools/qwen_index_until_done.py](../tools/qwen_index_until_done.py).
  Сделать: по спеке 2026-06-07 **с четырьмя обязательными правками** (Приложение А): архив оригиналов вместо удаления; state-файл идемпотентности (метабаза следит за mtime/size — qdrant_adapter.py:386); сплит по границам секций/закладок с жадной упаковкой по байтам; `part_index/part_total/original_name` в payload частей.
  Приёмка: тест-план спеки (~13 тестов) зелёный + тесты на 4 правки; `make verify`.

- [ ] **W1.4 Конвейер стадий с resume** · L
  Файлы: [backend/qdrant_adapter.py:919-1110](../backend/qdrant_adapter.py) (`_sync_parse` — монолит), метабаза (новые статусы стадий).
  Сделать: явные стадии preprocess→convert→chunk→embed→upsert со статусом per-file; рестарт продолжает с места падения; конвертация следующего файла параллельно с эмбеддингом текущего; per-file таймаут с постраничной батч-конвертацией PDF.
  Приёмка: kill -9 посреди индексации → повторный запуск доводит датасет без дублей точек; время датасета −30%+.

- [ ] **W1.5 Layout-aware парсер (Docling/MinerU)** · M `[dep]`
  Файлы: [backend/converter.py](../backend/converter.py), [backend/document_router.py](../backend/document_router.py) (выбор pipeline).
  Сделать: сравнить Docling vs MinerU на 10 наших документах с таблицами (кросс-страничные спецификации — ключевой кейс); победителя — опциональным конвертером (`RAG_PDF_PARSER=docling|mineru|pymupdf`), pymupdf4llm — fallback.
  Приёмка: таблицы из эталонных PDF извлечены без разрывов (ручная проверка по чек-листу); офлайн-тесты с фикстурами.

## Волна 2 — Качество ретрива `[gate:domain]` для всех задач

- [ ] **W2.1 Чанкинг в токенах** · M `[reindex]`
  Файлы: [backend/rag_config.py](../backend/rag_config.py) (chunk_size в токенах), `StructureAwareSplitter` в [backend/qdrant_adapter.py:47-212](../backend/qdrant_adapter.py), [mlx_host.py:89](../mlx_host.py) / env (`COREML_EMBED_SEQ_LEN`).
  Сделать: размеры чанков в токенах токенизатора профиля; ассерт при старте: chunk_tokens ≤ seq_len; `MIN_CHUNK` 20→100 символов; согласовать qwen-профиль (сейчас 1550 симв при seq_len 512 ток).
  Приёмка: ни один чанк эталонного корпуса не превышает seq_len (тест); golden 16/16; **реиндекс в одно согласованное окно вместе с W2.5**.

- [ ] **W2.2 Cross-encoder реранкер (замена LLM-реранкера)** · M `[dep]` `[live]`
  Файлы: [backend/reranker.py](../backend/reranker.py) (заменяется), [proxy/services/retrieval_service.py:352-420](../proxy/services/retrieval_service.py), [mlx_host.py](../mlx_host.py) (хостинг модели) или ONNX в proxy.
  Сделать: bge-reranker-v2-m3 или Qwen3-Reranker-0.6B как сервис `/v1/rerank`; LLM-реранкер удалить (ADR-3). Сопоставление результатов по `doc_id`/`chunk_ord`, не по равенству текста.
  Приёмка: латентность реранка 12 чанков < 0.5 с (было 2-6 с); golden не хуже; реранк не держит `llm_semaphore`.

- [ ] **W2.3 Гибрид + реранкер вместе** · M
  Файлы: [proxy/services/retrieval_service.py:352-420](../proxy/services/retrieval_service.py) (ранний return ветки реранкера минует `_hybrid_merge`).
  Сделать: единый путь: vector + sparse/lexical → RRF → пул 24-36 → rerank → top 6. Удалить ветвление «реранкер ИЛИ гибрид».
  Приёмка: `retrieval_trace.mode == "hybrid+rerank"`; golden + новые наборы W0.3 не хуже baseline.

- [ ] **W2.4 Sparse-вектора в Qdrant** · M `[reindex]`
  Файлы: [backend/qdrant_adapter.py](../backend/qdrant_adapter.py) (создание коллекции, upsert, retrieve), [mlx_host.py](../mlx_host.py) (sparse-выход эмбеддера).
  Сделать: named sparse vector (BGE-M3 sparse) рядом с dense; в ретриве — Qdrant hybrid query вместо самописного FTS-стеммера; FTS остаётся для clause lookup.
  Приёмка: запросы с точными терминами/артикулами (новый golden-поднабор) лучше baseline; зависимость W2.1 (общее окно реиндекса).

- [ ] **W2.5 Обогащение чанков** · S `[reindex]`
  Файлы: [backend/qdrant_adapter.py:252-315](../backend/qdrant_adapter.py).
  Сделать: пропагация последнего заголовка в чанки без своего; `heading_level` в payload; стабильный `parent_id` от `doc_id+chunk_ord`.
  Приёмка: доля чанков с пустым `section_heading` < 5% на эталонном корпусе (тест).

- [ ] **W2.6 Clause lookup v2** · S
  Файлы: [proxy/services/clause_lookup_service.py:13-18](../proxy/services/clause_lookup_service.py).
  Сделать: «раздел», «параграф», «приложение Б», римские цифры; запрос пункта 7.2 включает подпункты 7.2.x.
  Приёмка: новый golden-набор «пункты с подпунктами» из W0.3 проходит.

- [ ] **W2.7 Расширение запроса на weak-ретрае (алгоритмы → LLM)** · M `[live]`
  Файлы: [proxy/services/retrieval_service.py:439-447](../proxy/services/retrieval_service.py), `config/kot_terms.yaml`.
  Сделать (порядок по ADR-11): сначала расширить словарь синонимов KOT и морфологические варианты (бесплатно, детерминированно); LLM-перефраз (2-3 варианта, merge RRF) — только если словарный ретрай тоже дал weak. Замерить, какая доля weak закрывается словарём — возможно, LLM-ступень не нужна вовсе.
  Приёмка: доля weak-итогов на golden-прогоне снижается; латентность happy-path не растёт; в отчёте задачи — статистика «закрыто словарём vs LLM».

## Волна 3 — Слой инференса

- [ ] **W3.1 Протоколы провайдеров** · M
  Файлы: новый пакет `backend/inference/` (`ChatProvider`, `EmbedProvider`, `RerankProvider`, `OCRProvider`); рефактор вызовов в [proxy/services/runtime_dispatcher.py](../proxy/services/runtime_dispatcher.py), `EmbedClient` в qdrant_adapter.
  Сделать: текущий MLX-путь — первая реализация протоколов; поведение бит-в-бит как раньше.
  Приёмка: `make verify`; полная сюита на живом MLX без регрессий; диффы вызывающего кода минимальны.

- [ ] **W3.2 Провайдеры ollama / openai_compatible** · M `[live]`
  Файлы: `backend/inference/ollama.py`, `backend/inference/openai_compat.py`; конфиг профилей `config/profiles/*.yaml`.
  Сделать: chat+embeddings+VLM-OCR через Ollama; generic OpenAI-совместимый клиент (llama.cpp, LM Studio, vLLM); fallback-цепочки в конфиге (`chat: [mlx, ollama]`).
  Приёмка: golden-прогон на ollama-профиле (вторая машина или локально); матрица возможностей в `/api/runtime`.

- [ ] **W3.3 Облачные провайдеры (OpenRouter + OpenAI) + политика маршрутизации** · M
  Файлы: конфигурации поверх `backend/inference/openai_compat.py` из W3.2 (отдельный SDK не нужен — ADR-9: оба провайдера OpenAI-совместимы); `proxy/config.py` (флаг чувствительности датасета P0/P1/P2); политика в диспетчере; для OpenAI — поддержка Batch API в офлайн-конвейерах.
  Сделать: профили `cloud-openrouter` (модель — параметр конфига, дефолт GPT-5.5/GPT-5.4 из «Рекомендаций») и `cloud-openai` (то же + Batch); vision и structured outputs через тот же протокол; стабильные префиксы промптов под автокэш OpenAI; матрица маршрутизации задача×чувствительность; учёт расходов (токены/$ в метриках W0.1); деградация на локальный провайдер при недоступности облака. Ключи API — только из env, не логировать.
  Приёмка: задача с P0-датасетом физически не может уйти в облако (тест); счётчик расходов в `/api/metrics`; смена облачной модели — правкой конфига без кода; при выключенной сети всё работает локально как раньше.
  **Задел (2026-06-11):** переключение провайдера уже работает без рестарта — `_llm_runtime()` в chat.py (mlx/ollama/openrouter/openai) + `/api/settings` (env на лету + персист в .env) + селектор в Lite Admin (`/les` → LLM Provider). Остаток W3.3 — политика P0/P1/P2, учёт расходов, fallback-цепочки и **memory-aware routing** (полевой вывод: ollama-модель рядом с MLX выедает RAM — диспетчер обязан смотреть на память до выбора локального провайдера).

- [ ] **W3.4 Валидация в proxy, каскад rules→LLM** · M
  Файлы: `/api/validate` из [mlx_host.py:1150-1262](../mlx_host.py) → сервис в proxy; rules-бэкенд уже существует (один из `VALIDATOR_BACKEND`).
  Сделать: каскад: rules (<100 мс) → LLM-валидатор только при неопределённом вердикте; structured-вердикт (constrained output) вместо парсинга свободного текста. **Дефолт рантайма не менять** — каскад как новая опция.
  Приёмка: validator golden set; доля ложных HALLUCINATION не выше baseline; латентность валидации на простых вопросах < 0.3 с.

## Волна 4 — Мультиплатформа

- [ ] **W4.1 Кроссплатформенный супервизор lesctl** · L
  Файлы: [tools/lesctl.py](../tools/lesctl.py), [tools/les_runtime_control.py](../tools/les_runtime_control.py), `installers/`.
  Сделать: start/stop/health/автоперезапуск через адаптеры launchd (Mac) / NSSM или Планировщик (Win) / systemd (Linux).
  Приёмка: `lesctl up && lesctl status` поднимает стек на чистых Mac и Win VM.

- [ ] **W4.2 Windows-профиль рантайма** · M `[live]`
  Сделать: профиль `win-ollama` (chat/OCR через Ollama, эмбеддер sentence-transformers/ONNX); аудит путей (pathlib, русские имена `_частьN.pdf` на NTFS, длинные пути).
  Приёмка: golden-прогон на Win; `make verify` на Win.

- [ ] **W4.3 CI-матрица** · S
  Сделать: W0.2 workflow → matrix macos+windows.
  Приёмка: оба зелёные.

## Волна 5 — Стриминг и UI

- [ ] **W5.1 SSE-стриминг чата end-to-end** · L `[live]`
  Файлы: [proxy/routers/chat.py:1210](../proxy/routers/chat.py) (`stream: False`), новый `/api/chat/stream`; [sovushka/lite_chat.py](../sovushka/lite_chat.py), [sovushka/pages/chat.py](../sovushka/pages/chat.py).
  Сделать: токены клиенту по мере генерации; вердикт валидации — финальным SSE-событием. Старый нестриминговый `/api/chat` сохранить (M5, смоуки, АРТЕЛЬ не трогаются).
  Приёмка: первый токен в UI < 2 с от отправки; `chat_format_smoke` зелёный на старом эндпоинте.

- [ ] **W5.2 Push `/api/live` вместо поллинга** · M
  Файлы: [sovushka/state.py:331-354](../sovushka/state.py) (bg_loop), таймеры [samovar.py:834](../sovushka/pages/samovar.py), [prorab.py:513](../sovushka/pages/prorab.py), [chat.py:1102](../sovushka/pages/chat.py).
  Сделать: один SSE-канал (метрики, статусы job, прогресс «документ 37/120»); таймеры — на подписку.
  Приёмка: ≤ 2 фоновых HTTP-запроса/мин на открытую вкладку (было десятки); прогресс-бар индексации в САМОВАРе.

- [ ] **W5.3 Quick wins UI** · S
  Файлы: [sovushka/state.py:193](../sovushka/state.py) (полное чтение лога), [pages/chat.py:521-529](../sovushka/pages/chat.py) (`chat_column.clear()`), [state.py:91-119](../sovushka/state.py).
  Сделать: tail-чтение логов (seek с конца); инкрементальный рендер сообщений; TTL-кэш `api_get`; индикатор «proxy недоступен»; честное сообщение при таймауте 90 с.
  Приёмка: ручной чек-лист + `browser_smoke`.

- [ ] **W5.4 Единый чат-шелл** · L
  Файлы: [sovushka/pages/chat.py](../sovushka/pages/chat.py) (1107 строк) vs [sovushka/lite_chat.py](../sovushka/lite_chat.py) (844).
  Сделать: lite_chat — основной; NiceGUI-чат — тонкая обёртка над тем же REST/SSE-контрактом. Делать ПОСЛЕ W5.1 (контракт устоялся).
  Приёмка: одна реализация парсинга ответа/источников/фидбека; smoke оба шелла.

## Волна 6 — Домены и новые техники

- [ ] **W6.1 Агрегатные BIM-чанки** · M `[gate:domain]`
  Файлы: [proxy/services/cad_bim_graph.py:507-608](../proxy/services/cad_bim_graph.py) (`render_projection`).
  Сделать: сводные чанки «этаж × категория × система» (количество, перечень, общие свойства) поверх поэлементных.
  Приёмка: BIM-набор из W0.3 («какие воздуховоды на 3 этаже») проходит.

- [ ] **W6.2 Покрытие больших моделей CAD/BIM** · M
  Файлы: [cad_bim_graph.py:182](../proxy/services/cad_bim_graph.py) (max_objects=5000), `_walk_payload`.
  Сделать: приоритизация при импорте (типы+агрегаты раньше инстансов), фильтр по категориям/этажам в API, лог «обработано X из Y». Делать ПОСЛЕ W6.1 (иначе 100k плоских чанков ухудшат ретрив).
  Приёмка: импорт 100k-модели даёт осмысленное покрытие, отбрасывание залогировано.

- [ ] **W6.3 АРТЕЛЬ: чанки и версионирование** · M
  Файлы: [tools/seed_artel_fop_profiles.py](../tools/seed_artel_fop_profiles.py) (чанк на параметр), [tools/seed_artel_revit_factory_sources.py](../tools/seed_artel_revit_factory_sources.py) (шардирование SDK), [schema/artel_family_learning_case.schema.json](../schema/artel_family_learning_case.schema.json) (`version/supersedes/deprecated_at`), batch-seed (`--batch-dir`, один sync).
  Приёмка: тесты `test_artel_*`; смоук `tools/smoke_artel_expert_loop.py`; АРТЕЛЬ-набор из W0.3.

- [ ] **W6.4 Визуальный индекс сканов (ColPali/ColQwen)** · L `[dep]` `[live]`
  Файлы: новая коллекция Qdrant (мультивекторы), новый pipeline в [backend/document_router.py](../backend/document_router.py) для `needs_ocr`-документов, ретрив-ветка в retrieval_service.
  Сделать: эксперимент на 20 сканах (ГОСТ-сканы, листы РД со штампами): page-image эмбеддинги ColQwen vs текущий OCR-путь; при выигрыше — прод-ветка «scan → визуальный индекс», OCR остаётся для извлечения текста в ответ.
  Приёмка: side-by-side на скан-наборе golden; решение go/no-go задокументировать здесь.

- [ ] **W6.5 Пилот агентного ретрива (multi-hop)** · L `[live]`
  Сделать: для вопросов с перекрёстными ссылками (СП→ГОСТ) router включает итеративный режим: LLM получает инструмент поиска, ≤ 3 прыжков. Селективно, не дефолт.
  Приёмка: новый golden-поднабор multi-hop; латентность обычных вопросов не затронута.

- [ ] **W6.6 Эксперимент LightRAG** · L `[dep]`
  Сделать: граф сущностей нормативов (у нас уже есть `structured_rules` + `rules_extractor`) → LightRAG-style dual-level retrieval на поднаборе НТД. Только эксперимент с отчётом, не интеграция.
  Приёмка: отчёт со сравнением на golden; go/no-go здесь.

## Волна 8 — Полевой ввод: рукописные пометки → учёт объёмов

Новый модуль. Сценарий: со стройки приходят фото/сканы чертежей с рукописными пометками (выполненные объёмы, исправления, замечания) — система извлекает из них структурированные данные и ведёт журнал объёмов, по которому можно спрашивать в чате («сколько уложено бетона по захватке 2 за неделю»).

- [ ] **W8.1 Журнал объёмов (модель данных)** · M
  Файлы: новые таблицы в метабазе (`field_entries`: дата, фото-источник, позиция/элемент, объём, ед.изм., автор, статус confirmed/pending, связь с doc_id чертежа и element_id BIM-графа); новый `proxy/services/field_intake_service.py`; роутер `proxy/routers/field.py`.
  Приёмка: CRUD журнала через API; запись ссылается на чертёж/элемент; `make verify`.

- [ ] **W8.2 Извлечение пометок (VLM-конвейер с алгоритмической предобработкой)** · L `[dep]` `[live]`
  Зависит: W3.3 (облачный провайдер).
  Сделать (ADR-11 — VLM получает минимум работы): ступень 1 БЕЗ LLM — дедупликация фото по perceptual hash; поворот/контраст/выравнивание (OpenCV); детекция зон рукописных пометок классическим CV (цвет чернил/diff с чистым листом, если он в индексе). Ступень 2 — локальная **Gemma 4 12B** классифицирует кропы (число/подпись/штамп/мусор), мусор отсекается. Ступень 3 — в облако уходят только **кропы с данными**: GPT-5.5 vision (OpenAI/OpenRouter) со structured output `{позиция, значение, единица, привязка, confidence}` → pending. Пакетно через OpenAI Batch (−50%). При недоступности облака очередь ждёт (ADR-10).
  Приёмка: на наборе ≥20 реальных фото — точность извлечения чисел зафиксирована здесь; доля фото/площади, отсечённой до VLM, в отчёте; ни одна запись в журнале без статуса.

- [ ] **W8.3 Подтверждение человеком (review-UI)** · M
  Сделать: экран в Совушке: фото с подсветкой зоны пометки рядом с извлечёнными полями → подтвердить/исправить/отклонить; только confirmed-записи попадают в отчёты и чат. Исправления сохраняются как пары (фото-фрагмент → верное значение) — будущий датасет для дообучения/few-shot.
  Приёмка: цикл фото→извлечение→подтверждение→запрос в чате проходит вручную end-to-end.

- [ ] **W8.4 Запросы и отчёты по объёмам** · M
  Сделать: канал `field` в query_router (токены «объём», «выполнено», «захватка»…); агрегации — **только SQL** (ADR-11: LLM числа не считает); ответ — шаблонами (таблица/сводка без LLM); LLM подключается единственно для разбора свободной формулировки вопроса в параметры запроса (период/захватка/позиция), и то после попытки regex-разбора. Экспорт в xlsx.
  Приёмка: golden-набор вопросов по объёмам; суммы в ответах сходятся с SQL бит-в-бит; типовые вопросы («сколько X за период Y») отвечаются вообще без LLM-вызова (тест).

## Волна 9 — Норматив ↔ чертёж/модель

Сопоставление требований НТД элементам CAD/BIM: «какие требования СП применимы к этому воздуховоду», «что в модели нарушает п. 7.3».

- [ ] **W9.1 Применимость норм к элементу** · L
  Зависит: W6.1 (агрегатные чанки), таблица `structured_rules` (уже есть, бережём — guardrail).
  Файлы: новый `proxy/services/norm_matching_service.py`; расширение `/api/cad-bim/element` (rag_prompt уже есть — добавить блок применимых норм).
  Сделать (ADR-11): первая ступень — **детерминированная таблица соответствия** «категория/система элемента → разделы НТД» (curated-маппинг, ~сотни строк, версионируется в config/) + выборка из `structured_rules` по ней; LLM-проверка применимости — только для элементов вне маппинга, и её вердикт **пишется обратно в маппинг** (кэш на пару тип×норма), так что доля LLM-вызовов убывает со временем к нулю.
  Приёмка: для 10 эталонных элементов список норм проверен экспертом и зафиксирован как golden-набор; повторный прогон того же импорта — 0 LLM-вызовов (тест кэша).

- [ ] **W9.2 Проверка модели на соответствие (нормоконтроль ЦИМ-lite)** · L `[live]`
  Сделать (ADR-11): требования из `structured_rules` нормализуются в **машинно-проверяемые предикаты** (параметр, оператор, значение, единица) один раз при извлечении; пакетный проход по модели — чистое сравнение предикатов с параметрами элементов, без LLM. LLM участвует только в офлайн-нормализации требований, которые не разобрались парсером (и результат сохраняется как предикат). Отчёт «элемент → норма → статус → цитата пункта».
  Приёмка: отчёт по тестовой модели; прогон проверки по уже нормализованным правилам — 0 LLM-вызовов; ложноположительные на эталоне ≤ согласованного порога.

- [ ] **W9.3 Связь из вьювера** · M
  Сделать: клик по элементу во вьювере → в карточке контекста (lite-api/cad-bim/element) показываются применимые нормы со ссылками на пункты; вопрос из selection уходит с этим контекстом.
  Приёмка: ручной сценарий во вьювере; `test_cad_bim_viewer_selection_rag` расширен.

## Волна 10 — Фабрика семейств Revit (АРТЕЛЬ: от валидации к генерации)

Текущее состояние: плагин умеет extract/validate + безопасные действия (параметры, типы); экспертный цикл замкнут (W6.3). Шаг — генерация: задание → спецификация → план действий → исполнение плагином → валидация → learning case.

- [ ] **W10.1 Генератор спецификации семейства** · L `[dep]`
  Зависит: W3.3 (облако), W6.3 (ФОП-чанки, версионирование кейсов).
  Файлы: products/artel/backend (endpoint `ai-analysis` сейчас skeleton — наполнить); схема `family_spec.v1` в schema/.
  Сделать: задание + RAG-контекст (ФОП-параметры, похожие learning cases, FAMILY_GUIDE) → GPT-5.5 (через OpenRouter — позволяет A/B с альтернативами на тех же заданиях) structured output: спецификация семейства (категория, параметры с GUID из ФОП, типоразмеры, ограничения геометрии). Справочник ФОП — стабильным префиксом промпта под автокэш OpenAI. Спецификация — на ревью BIM-менеджеру (существующий workflow approve).
  Приёмка: для 5 эталонных заданий спецификации приняты BIM-менеджером с ≤2 правками каждая.

- [ ] **W10.2 План действий Revit API** · L
  Сделать: утверждённая спецификация → план операций **компилируется детерминированно** (ADR-11: спецификация структурирована — маппинг «параметр спецификации → add shared parameter c GUID из ФОП», «типоразмер → create type» это код, не LLM) + список ручной работы (геометрия). Валидация плана против schema `family_action_plan.v1` и справочника ФОП — до отправки в Revit. Плагин исполняет план батчем и пишет validation report.
  Приёмка: план для эталонного «шкафа» компилируется без LLM, исполняется плагином на Legion без ошибок; report попадает в learning case автоматически.

- [ ] **W10.3 Итеративный цикл генерации** · L `[live]`
  Сделать: validation report с ошибками → возврат в генератор (контекст: спецификация + ошибки + похожие fixes из learning cases) → исправленный план → повтор (≤3 итераций). Каждая итерация логируется в learning case (`known_failures`/`fixes` — структура уже есть).
  Приёмка: смоук `smoke_artel_expert_loop` расширен сценарием генерации; доля семейств, проходящих валидацию ≤3 итераций, измерена на эталонных заданиях.

## Волна 11 — Ведомости объёмов работ (ВОР) из спецификаций

Чистый ADR-11-модуль: ноль LLM в ядре. Конвейер таблиц уже есть (Parquet, [backend/parquet_writer.py](../backend/parquet_writer.py), `table_query_service`).

- [x] **W11.1 Генератор ВОР** · M — 2026-06-10. `proxy/services/bor_service.py` (Parquet→свод→xlsx, 0 LLM: тест на импорты) + роутер `proxy/routers/bor.py` (`/api/bor/{id}/preview|generate|download`). Группировка раздел×наименование×код×марка×ед.изм., `UNIT_ALIASES`-нормализация единиц, учёт строк без количества, источники `файл#позиция`. 13 офлайн-тестов (tests/test_bor_service.py), полный цикл на синтетическом Parquet. **Остаток приёмки `[live]`:** сверка по 2-3 реальным спецификациям из RAG_Content с оператором.

- [ ] **W11.2 ВОР в UI + план/факт** · M
  Зависит: W11.1 (готово; API есть); для план/факт — W8.1 (журнал объёмов).
  Сделать: кнопка в Совушке (preview + download); сравнение «ВОР (план) ↔ журнал полевых объёмов (факт)» с дельтами — SQL.
  Приёмка: ручной сценарий спецификация→ВОР→xlsx из UI; план/факт-отчёт на тестовых данных.

- [ ] **W11.3 Функция «образец»: заполнение документов по шаблону (xlsx / docx / pdf)** · L
  Канон ADR-11: «документы = шаблоны + данные» ([llm-boundaries](https://ai.ovc.me/llm-boundaries)).
  Общий механизм: оператор загружает **образец** (документ в форме его организации) → система один раз строит **карту образца** (`config/templates/<имя>.yaml`: где какие поля/колонки) детерминированным сопоставлением (словарь `_map_columns_simple` из parquet_writer + метки в тексте); непознанные поля — LLM-маппинг один раз, результат сохраняется в карту; **повторное заполнение того же образца — 0 LLM** (тест). Карты и образцы версионируются как файлы. Заполнение — всегда детерминированное.
  Фазы по форматам:
  - **(a) xlsx** · M — копия файла, запись в data-зону с сохранением шапки/стилей (openpyxl, уже в deps). Источники данных: ВОР, спецификации, журнал объёмов.
  - **(b) docx** · M `[dep]` — два режима: таблицы (заполнение строк существующей таблицы образца с сохранением стилей) и поля-метки (`{{поле}}` в тексте — титульники, акты). Нужен python-docx — согласовать `uv add`.
  - **(c) pdf** · M — два режима через pymupdf (уже в deps): AcroForm-поля (если бланк интерактивный — заполнение полей по именам) и координатная разметка «плоского» бланка (зоны размечаются один раз в карте образца, текст накладывается по координатам).
  Приёмка: по одному реальному образцу оператора на формат — выгрузка с сохранением его оформления; повторная выгрузка — 0 LLM-вызовов (тест); числа сходятся с источником бит-в-бит (для ВОР — с `/api/bor/preview`).

## Волна 12 — Поиск различий в чертежах и документах

- [x] **W12.1 Дифф CAD-графов и текстовых документов** · M — 2026-06-10. `proxy/services/diff_service.py` + роутер `/api/diff/cad-bim` (два импорта → added/removed/changed по source_id, поля+свойства) и `/api/diff/text` (сопоставление по пунктам ГОСТ/СП, difflib-сниппеты, нечувствительность к пробелам). 0 LLM (тест на импорты). 11 офлайн-тестов (tests/test_diff_service.py). **Остаток приёмки `[live]`:** дифф двух реальных ревизий модели/документа с оператором.

- [ ] **W12.2 Растровый дифф PDF-листов** · L
  Сделать (фаза 2): выравнивание листов (OpenCV: ORB/гомография) → попиксельный overlay с подсветкой зон изменений → привязка зон к рамке листа. Классический CV, без LLM.
  Приёмка: пары «лист до/после» из реальных ревизий; зоны изменений подсвечены, ложные срабатывания на сдвиг/скан-шум ≤ согласованного порога.

## Волна 13 — Нормоконтроль документации

- [x] **W13.1 Формальный нормоконтроль — v1** · M — 2026-06-10. `proxy/services/normcontrol_service.py` + `/api/normcontrol/{id}/run|download`: NK-01 форматы листов по ГОСТ 2.301 (вкл. кратные А4×N), NK-02 текстовый слой (сканы), NK-03 согласованность шифра комплекта, NK-04 ведомость чертежей (VEDOMOST в Parquet) ↔ фактический состав. Отчёт xlsx, severity-сортировка. 0 LLM (тест). 13 офлайн-тестов на синтетических PDF (fitz).
  **Остаток (v2):** графы основной надписи/подписи — нужен layout-анализ штампа; методика скилла normcontrol-pdf не найдена на диске (облачный скилл) — при переносе запросить у оператора чек-лист граф + эталонный комплект РД для приёмки. `[live]`-приёмка v1: прогон по реальному комплекту с оператором.

- [ ] **W13.2 Содержательный нормоконтроль** — это W9.2 (предикаты из structured_rules); отдельной задачи нет, ссылка для трассировки.

## Волна 14 — Текстовая часть проектов по XML-схемам

`[blocked: входные данные]` — нужны от оператора: версии XML-схем (Минстрой, разделы ПД), 2-3 эталонные пояснительные записки, источник данных проекта (BIM-граф/таблицы).

- [ ] **W14.1 Каркас по XSD** · M
  Сделать: парсинг XSD → дерево обязательных разделов/полей → шаблон документа; валидация готового XML против XSD — алгоритмическая (lxml). Данные проекта подставляются из BIM-графа и таблиц детерминированно где возможно.
  Приёмка: каркас по эталонной схеме валиден против XSD; обязательные поля размечены «данные есть / нужна генерация / нужен ввод».

- [ ] **W14.2 Генерация текстовых разделов** · L `[live]`
  Зависит: W3.3 (облако), W14.1.
  Сделать: по каждому разделу — RAG-контекст (нормы, данные проекта, эталонные формулировки) → облачная LLM (GPT-5.5/5.4) → текст раздела; сборка в документ (docx/XML), валидация против XSD; ревью человеком до выпуска. Стабильный префикс промпта (эталоны) под автокэш OpenAI.
  Приёмка: записка по эталонному заданию принята оператором с умеренными правками; XML валиден.

## Волна 7 — Установка и релиз

- [ ] **W7.1 Установка одной командой** · M
  Файлы: `installers/{macos,linux,windows}` + [tools/install_les.py](../tools/install_les.py) → единый `install.sh` / `install.ps1`.
  Сделать: uv → релиз → `uv sync` → выбор профиля (`mac-mlx`, `win-ollama`, `cpu-min`, `docker`) → регистрация сервисов (W4.1) → модели → smoke.
  Приёмка: чистая VM → работающий чат одной командой, обе платформы.

- [ ] **W7.2 lesctl doctor** · S
  Сделать: диагностика портов/RAM/диска/GPU/провайдеров/коллекций одним отчётом.
  Приёмка: ломаем по очереди qdrant/inference/proxy — doctor называет причину.

- [ ] **W7.3 Миграция ЛЕС 2 → ЛЕС 3** · M `[reindex]`
  Сделать: миграции схемы метабазы; конвертер env→профили; единое окно реиндекса коллекций (геометрия чанков W2.1/W2.4); чек-лист отката.
  Приёмка: прогон на копии прод-данных; golden 16/16 после миграции.

---

## Дорожная карта по приоритетам

Номер волны — идентификатор, не порядок. Порядок работ — очереди П1-П5. Внутри очереди задачи независимы и берутся параллельно. Очередь считается закрытой, когда её `[ ]` отмечены и гейты зелёные.

### П1 — Фундамент + быстрая ценность (сейчас, без блокеров, ~2 недели)

| Задача | Зачем первой |
|---|---|
| **W0.1-W0.3** метрики, CI, golden-наборы | Без измеримости нельзя принять ничего из последующего |
| **W1.1, W1.2** конфиг-твики, SQLite/батчи | Дешёвое ускорение индексации, без реиндекса |
| **W11.1** генератор ВОР | Готовая пользовательская ценность на существующем table-конвейере, ноль блокеров |
| **W12.1** дифф CAD-графов и текстов | То же: граф уже в SQLite, чистый SQL |
| **W13.1** формальный нормоконтроль | Методика готова (normcontrol-pdf), extraction есть |

### П2 — Качество ядра (~месяц)

| Задача | Примечание |
|---|---|
| **W1.3-W1.5** препроцессор PDF, конвейер с resume, layout-парсер | Стабильность индексации до реиндекса |
| **W2.1-W2.7** токенный чанкинг, cross-encoder реранкер, гибрид, sparse, заголовки, clause v2 | Главный скачок качества; W2.1+W2.4+W2.5 — **одно окно реиндекса** (согласовать) |
| **W6.1** агрегатные BIM-чанки | В то же окно; кормит W9/W13.2 |

### П3 — Провайдеры и отзывчивость (~месяц, частично параллельно П2)

| Задача | Примечание |
|---|---|
| **W3.1-W3.4** шина провайдеров, Ollama, **OpenRouter/OpenAI**, каскад валидации | W3.3 разблокирует все облачные продуктовые модули |
| **W5.1-W5.3** SSE-стриминг чата, push вместо поллинга, UI quick wins | Самый заметный пользователю выигрыш по ощущениям |

### П4 — Продуктовые модули (на готовом контуре П2+П3)

| Задача | Блокер |
|---|---|
| **W8.1-W8.4** полевой ввод: рукопись → журнал объёмов | W3.3 |
| **W11.2** ВОР план/факт | W8.1 |
| **W9.1-W9.3** норматив↔чертёж (+W13.2 содержательный нормоконтроль) | W6.1 |
| **W11.3** заполнение документов по образцу (xlsx/docx/pdf) | образцы от оператора по форматам; для docx — одобрить `uv add python-docx`. Сама задача алгоритмическая, можно раньше |
| **W14.1-W14.2** текстовая часть по XML-схемам | W3.3 + **входные данные от оператора** (схемы, эталоны) |
| **W10.1-W10.3** фабрика семейств | W3.3, W6.3 |
| **W6.3** АРТЕЛЬ: ФОП-чанки, версионирование кейсов | — (можно раньше, если АРТЕЛЬ горит) |
| **W12.2** растровый дифф PDF | — (фаза 2 диффа) |

### П5 — Платформа, эксперименты, релиз

| Задача | Примечание |
|---|---|
| **W4.1-W4.3** кроссплатформенный супервизор, Win-профиль, CI-матрица | Перед релизом, не раньше стабилизации контура |
| **W5.4** единый чат-шелл | После устаканивания SSE-контракта |
| **W6.2** покрытие больших BIM-моделей; **W6.4-W6.6** ColPali/агентный ретрив/LightRAG | Эксперименты с go/no-go |
| **W7.1-W7.3** установка одной командой, doctor, миграция ЛЕС2→ЛЕС3 | Закрывает релиз |

### Граф жёстких зависимостей

```
W0 ──► все
W1 ──► W2 (конвейер до реиндекса) ──► W6.1/W6.2/W6.4
W2.1 + W2.4 + W2.5 + W6.1 — ОДНО окно реиндекса (согласовать с оператором)
W3 ──► W4, W5.1, W6.5
W3.3 (облако) ──► W8.2, W10.1, W14.2
W6.1 ──► W9.1 ──► W9.2 (=W13.2) / W9.3
W6.3 ──► W10.1 ──► W10.2 ──► W10.3
W8.1 ──► W11.2 (план/факт)
W5.1 ──► W5.4
W7 — последняя
```

Живой рантайм ЛЕС 2 не трогается до W7: разработка в ветках, новые сервисы — на отдельных портах.

---

## Приложение А — правки к спеке PDF-препроцессора (обязательны для W1.3)

Спека 2026-06-07 принята. Четыре правки:

1. **Оригиналы не удалять**: перемещение в `_originals/` (флаг `--archive-dir`); удаление — только явным `--delete-originals`. Причина: guardrail «беречь RAG_Content».
2. **State-файл идемпотентности** (`.pdf_preprocess_state.json`: path→mtime,size,hash): очистка меняет mtime/size, метабаза по ним отслеживает документы ([qdrant_adapter.py:386](../backend/qdrant_adapter.py)) — без state повторный прогон переиндексирует уже готовые файлы.
3. **Сплит по структуре**: жадная упаковка страниц по фактическим байтам + предпочтение границ закладок/секций; в payload частей `part_index/part_total/original_name` (источники в чате склеиваются, clause lookup находит семейство по `doc_name LIKE`).
4. **Параллельно лечить причину**: per-file таймаут с постраничной батч-конвертацией PDF — в W1.4; препроцессор — оптимизация, не обязательный костыль.

Остальное (CLI, коды выхода 0/1/2, atomic tmp→rename, JSON-лог, тест-план ~13 тестов, имена `_частьN.pdf`) — как в спеке.
