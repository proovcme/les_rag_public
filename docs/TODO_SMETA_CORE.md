# TODO — живое сметное ядро LES

> Канонический долг после жёсткого аудита 2026-07-10. Проверять перед каждой сметной правкой.
> Цель: проектные источники → ВОР → явный выбор модели → ресурсы → цены → ЛСР с деньгами и provenance.

> **Архитектурное закрытие 2026-07-12:** старый per-row orchestrator, отдельные
> `finish_norm_selection`, resource/impact/dominant review и недостижимое legacy-тело удалены.
> Живой dev-контракт: `search_norms_batch → read_norms_batch → submit_lsr_mapping → один расчёт → XLSX`.
> Тест контракта обрабатывает 50 исходных строк одним model-owned submission. Полный test/release
> gate и Win/macOS сборки закрыты. Следующие обязательные шаги: живой zero-state
> ЛСР, deploy и проверка восстановления артефакта из истории.

> **Application boundary 2026-07-12:** новые сметные потребители входят только через
> `proxy.smeta_core.application`. API ЛСР и artifact builder переведены на facade;
> `estimate_harness_service` остаётся временным private adapter за этой границей.
> Старые `construction_harness_service`/`unified_construction_harness_service` помечены
> `LEGACY_PRIVATE`, а их evidence-fixture больше не выбирает top-1 ГЭСН кодом.

> **Гейт пользователя:** до живой ЛСР не запускать новые тестовые/релизные ритуалы, не обновлять
> версию, не деплоить и не пушить. Каждый прогон БАП начинается как первый: новый scenario/revision,
> пустые model decisions и resource bindings, без чтения прошлой БАП-ревизии, без case-specific
> правил, кэша выбранных норм или переноса прошлых ответов. Повторный прогон обязан заново получить
> решения через универсальные LES tools; старый результат используется только внешним рецензентом.

> **Исторический live-гейт 2026-07-10:** ручной Codex-проход ниже доказал калькулятор/форму, но не штатный чат.
> Лучший штатный zero-state chat-run на 2026-07-10: `priced_partial`, 7/19 прямых норм,
> 33 562,42 руб. без НДС, 40 946,15 руб. с НДС; 12 строк открыты. Это не принято как живая ЛСР.
> Корень текущего ремонта: SQLite-only lexical retrieval нормативных карточек заменяется dedicated
> hybrid RAG; каждый новый БАП по-прежнему идёт с новым attachment/scenario без prepared decisions.

> **Исторический срез 2026-07-11 — первые БАП из чата:** принудительные `response_format + submit_json tool_choice`,
> из-за которых OpenAI-compatible gateway отвечал `400`, удалены. Исправлено и отдельное применение
> `LES_SMETA_DOCUMENT_MODEL`: до ремонта UI показывал GPT-5.4, а workflow фактически продолжал вызывать
> глобальный GLM-5.2. Три полностью zero-state запуска по одному PDF доказали, что transport теперь живой,
> но приёмка ядра не закрыта: лучший штатный результат GLM-5.2 — `priced_partial`, 14 bound + 3 covered +
> 2 open, 277 960,07 руб. без НДС / 339 111,29 руб. с НДС; фактический GPT-5.4 — 13 bound + 2 covered +
> 4 open, 124 558,78 / 151 961,71 руб. Главный дефект теперь не API модели, а нестабильный norm retrieval и
> многошаговый batch/state-machine: разные модели получают существенно разные нормы и итоги из одного ВОР.
> Передаваемый live-артефакт лучшего штатного прогона: `outputs/bap-live/LSR_BAP_live_2026-07-11.xlsx`.
> Следующий ремонт: один диалог модели с нативными `search_norms/read_norm/calculate_lsr`, без независимых
> batch-turns; затем тот же БАП с нуля должен дать повторяемое покрытие и только после этого — СКС.

> **Промежуточный успех 2026-07-11:** native agent loop прошёл полный zero-state БАП без удвоения
> количеств: 19 строк сохранены, 14 рассчитаны нормами, 5 оставлены незакрытыми; стоимость известной
> части 290 765,73 руб. без НДС / 354 734,19 руб. с НДС. Формулы и XLSX целы, но профессиональная
> ЛСР оценивается примерно в 4/10: ограничения аналогов пока не превращаются в resource decisions,
> missing цены и ЗПМ арифметически дают ноль внутри обычного итога позиции. Цель следующего ремонта —
> **7/10 по ЛСР**, сохранив raw model result и не возвращая code-side выбор норм.

## Цель 7/10 после промежуточного успеха

Текущий ремонт перед следующим zero-state БАП:

- [x] Ограничить транспорт доказательств native-agent: состояние поиска хранится вне диалога,
  полная карточка нормы доставляется модели один раз и повторно открывается только по `read_norm`.
- [x] Кешировать точные повторные `search_norms` и rerank внутри одного расчёта; reranker запускается
  только по явному запросу модели для спорной короткой выдачи.
- [x] Держать активный snapshot ограниченным: последние кандидаты и краткие решения вместо всей
  истории поиска; при исчерпании мягкого бюджета модель выбирает просмотренное, оставляет строку
  незакрытой либо явно обосновывает новый поиск.
- [ ] Повторить БАП и сравнить с baseline 2026-07-11: 20 ходов, 154 запроса, 283 078 символов
  активного контекста, 170,3 с reranker, около 16 минут целиком.

Причина 17-минутного прогона локализована по завершённому trace: один пользовательский запрос
разворачивался в 33 последовательных вызова GPT-5.4 (`20` native-agent + `2` resource review +
`10` отдельных FGIS price choice + `1` dominant review). Ремонт: FGIS-выбор пакетируется по 20,
resource review пакетируется по 20, явно открытые карточки доставляются до 32 за следующий ход,
native-agent ограничен 12 транспортными ходами, точные повторы кешируются. Бюджет исполнения
масштабируется по 20 строк: тестовый контракт для 50 строк — 3 пакета, не объектный БАП-костыль.

**Архитектурная правка 2026-07-12:** откат до немедленного расчёта полного состава аналога дал
формально аккуратную, но недостоверную ЛСР: пустой resource review автоматически превращался в
деньги. Новый инвариант не возвращает code-side selector: первичный mapping сохраняется immutable,
код считает факты, затем та же модель подтверждает труд/машины/материалы или задаёт
`add|replace|exclude|reuse`; основной XLSX строится по последней модельной ревизии. Код не выбирает
норму и не объявляет пустой ответ профессиональным согласием.

- [x] Ввести раздельное model-owned подтверждение труда, машин и материалов выбранной нормы;
  неподтверждённый компонент не считается полной подтверждённой стоимостью, но остаётся видимым в
  известной арифметической части до явного model-owned `exclude|replace|add|reuse`.
- [ ] Нормализовать составные измерители ГЭСН (`100 м труб`, `100 м кабеля` и аналогичные)
  без ослабления проверки размерности.
- [ ] Дать модели фильтруемую навигацию retrieval по семейству и сборнику, чтобы ЭОМ-поиск не
  смешивал обычные внутренние сети со специальными установками.
- [ ] Передавать исходное имя вложения и доступные реквизиты в шапку ЛСР вместо `read_...`.
- [ ] Сделать blockers/warnings листа `Проверка` читаемой таблицей.
- [x] После удаления code-side component filter повторить БАП с нуля; GPT-5.4, 0.24.0.379:
  `14 bound + 1 covered + 4 open`, 354 858,53 руб. без НДС. Скрытые нули устранены.
- [ ] Вернуть приёмочное покрытие не ниже `17/19` профессиональным поиском модели: в прогоне
  0.24.0.379 открыты проём ГКЛ, подготовка отделки, демонтаж потолка и прокладка кабеля. Не закрывать
  их кодовым selector/boost; улучшать прозрачность и полноту сметного RAG для самой модели.
- [ ] Заменить слабый аналог БАП только новой model-owned ревизией: текущий ГЭСН10-02-016-06 даёт
  273 172,51 руб. и доминирует в известной части; код не имеет права снять или заменить его сам.
- [x] Пагинировать model-facing candidate menu по 4–6 карточек с явной `page`; следующую страницу
  выбирает модель, код не фиксирует число просмотренных кандидатов.
- [x] Выгружать старые подробные tool results между ходами и сохранять компактный snapshot решений,
  незакрытых строк и открытых карточек; regression на 19 строк держит prompt ниже 120 000 символов.
- [x] Передавать `rerank=false` явно и не умножать batch timing на число запросов.
- [x] Разрешить impact review явно вызвать `next_action=reopen_norm`: новая норма снова выбирается
  моделью через search/read/bind и сохраняется отдельной mapping-ревизией.
- [x] Устранить механическое задвоение альтернативных представлений труда: при наличии разрядной
  детализации `2-100-*` не складывать её со средним разрядом `1-100-*` и текстовой строкой «всего».
- [x] Закрыть повторное появление сырого состава после resource review: `resources=[]` является
  явной пустой ревизией, а гидратация нормы разрешена только при отсутствии поля; последний renderer
  повторяет механическую нормализацию труда.
- [x] Не терять длинный ответ ресурсной проверки целиком при обрезанном JSON-хвосте: завершённые
  строки принимаются как решения модели, повторный вызов получает только недостающие `work_id`.
- [x] Сохранять XLSX-download contract в истории чата и восстанавливать карточку файла при открытии
  сессии; новый ответ и история используют один артефакт, без повторного расчёта.
- [x] Добавить единое публичное обновление ФГИС ЦС: каталог, последняя Сплит-форма каждой ценовой зоны
  и ГЭСН pipeline одной фоновой задачей; защищённые Bearer/captcha-разделы не обходить.

- [x] Разделить машинный контракт и ответ пользователю: внутренние статусы остаются в payload/trace,
  чат показывает один человеческий абзац, русские суммы и «стоимость рассчитанной части».
- [x] Для каждого `analog_norm` требовать явный model-owned resource review:
  `keep_all_confirmed | actions_confirmed | unresolved`; пустой список actions сам по себе не означает
  ни подтверждение, ни отказ.
- [x] Исполнять model-owned `exclude/replace/add/reuse` до расчёта денег и показывать решения на листе
  `Проверка`; код не определяет лишние ресурсы самостоятельно.
- [x] Разделить `known_amount` и `full_amount`: при missing цене или ставке ЗПМ известная часть видна,
  полная стоимость позиции и всей ВОР остаётся `null`, а обычный `Всего` не выглядит завершённым.
- [x] Закрыть ФСЭМ/ЗПМ для используемых машин либо оставить соответствующую часть стоимости неизвестной:
  активный каталог разрешает проверенные коды БАП; отсутствующее/несогласованное сопоставление делает
  `full_amount=null`, а не нулевую ОТм.
- [x] Устранить расхождение между текстом решения и binding: `bind_norm` требует `decision=selected`
  и машинный conclusion `applicable|applicable_with_limitations`; для отказа остаётся `leave_unbound`.
  иметь статус `bound` и участвовать в деньгах.
- [ ] Повторить БАП с нуля: 19/19 строк видимы, основные работы БАП закрыты или честно объяснены,
  аналоги имеют завершённый resource review, известная и полная стоимость различаются.

Реализация 0.24.0.357 также подключает к живому native-agent прямую reference-карту
`skills/smeta/references/gesn-storage.md`: identity нормы, typed SQLite, hybrid sibling,
source→norm→resource quantities, ФСЭМ и ценовые границы. До нового БАП это кодовый гейт, а не
утверждение о достигнутом качестве 7/10.

Live-проверка 0.24.0.357:

- первый GPT-5.4 прогон завершён за 207,7 с: 19 строк сохранены, 8 bound, 11 open,
  известная часть 35 174,65 руб. без НДС / 42 913,07 руб. с НДС; `full_amount=null`;
- прогон честно выявил неприменимые ресурсы, но показал два системных разрыва: runtime не имел
  `fsem_2022.sqlite`, а resource-review prompt не задавал точные поля машинного action, из-за чего
  правильные по смыслу replace/exclude/add были отвергнуты формальным контрактом;
- оба разрыва исправлены: ФСЭМ 1 576 строк зарегистрирован как service source, action schema названа
  явно и покрыта тестом, разумный аналог разрешён модели с последующей ресурсной правкой;
- второй zero-state GPT-5.4 прогон остановлен провайдером на третьем ходу с HTTP 402 Payment Required.

**Напоминание:** 7/10 не достигнут и задача не закрыта, пока после пополнения provod.ai не пройдёт
новый БАП с нуля. Не использовать первый прогон как prepared decisions и не подменять его аудиторской ЛСР.
- [ ] После удаления параллельных selector/reviewer/harness маршрутов сократить `SMETA_MECHANICS.md`:
  оставить одну runtime-схему, предметную механику и ссылки на историю вместо 600+ строк разных эпох.

## Неподвижные границы

- Модель/пользователь выбирает работы, нормы, аналоги, вариант и применимость.
- Код не выбирает норму. Код показывает карточки, валидирует формальные ограничения, раскрывает
  ресурсы, считает и хранит происхождение чисел.
- Первый полезный ответ содержит денежную ЛСР. Незаполненная цена остаётся строкой `0.00`/пусто с причиной.
- `answer`, `evidence_status` и `calculation_status` — разные статусы.
- Dataset/RAG заранее не предполагает состав файлов; читает фактический корпус.

## Нижняя граница после первого живого БАП

- Каждый следующий ремонт обязан сохранять 19/19 исходных строк в форме.
- Не ухудшать лучший штатный baseline: минимум 14 прямых норм + 3 доказуемых coverage; максимум 2 open.
- Нельзя сравнивать качество только по итоговой сумме: проверяются выбранные нормы, coverage, ресурсы,
  источники цен, НР/СП, НДС 22%, формулы и лист `Проверка`.
- Не приписывать лучший артефакт GPT-5.4: он получен GLM-5.2; GPT-5.4 в честном zero-state прогоне
  дал худшее покрытие. До стабилизации retrieval модель не считается лекарством архитектуры.
- Сохранять работающие intake, typed norm base, calculator, revision/provenance и XLSX renderer;
  заменяется только нестабильная оркестрация поиска/выбора норм.

## Фаза 0 — остановить ложную уверенность

- [x] Удалить fallback выбора первого сильного кандидата после невалидного ответа модели.
- [x] Запретить env-режимы не-модельной привязки нормы в основном harness.
- [x] Ввести отдельный semantic integrity report для нормативной machine base.
- [x] База без пройденного отчёта получает `quarantined_blocking`, не `ok`.
- [x] Structured-норма из карантина не может дать `priced_final`; сумма остаётся только наблюдаемым
  непроверенным черновиком.
- [x] Разделить `evidence_status` и `calculation_status` в `smeta_workflow_result_v1`.
- [x] User-facing from-rows/artifact/harness идут через canonical core; raw `/assemble` и
  `/lsr-trace` явно помечены `calculation_only`, `workflow_authority=false`, `evidence_status=not_asserted`.

## Фаза 1 — одно ядро

- [x] Создать `proxy/smeta_core/`: contracts, integrity, norm_browser, workflow.
- [x] Перевести auto smeta chat route на `run_smeta_workflow`.
- [x] Ввести `smeta_norm_browse_v1`: карточки без выбранного кодом шифра.
- [x] Синхронизировать quick price lookup с manifest-default pricebook.
- [x] Final authority для quantity/unit, formal norm validation, calculator orchestration и trace
  перенесён в core. Legacy `_add_position` остаётся адаптером natural-language planner, но его
  промежуточный `qty/status` повторно проверяется core и не является финальным решением.
- [x] Перевести direct model smeta, artifact и user-facing `/api/lsr/*` на одну finality policy.
- [x] Сохранять ревизии `WorkItem`, `NormBinding`, `NRSPBinding`, `PriceTrace`,
  `CoefficientTrace`, `LSRScenario`, `LSRRevision`.

Текущий факт: canonical `/api/lsr/*/from-rows` уже пишет immutable `LSRRevision`; общий
append-only store создан. Осталось убрать дубли расчёта из legacy harness и гарантировать ревизию
для каждого chat/artifact входа, а не только API.

## Фаза 2 — пересобрать нормативные данные

- [x] Не чинить текущий `gesn2022_unified.parquet` на месте; оставить в quarantine/audit scope.
- [x] Импортировать `ГЭСН`, `ГЭСНм`, `ГЭСНмр`, `ГЭСНп`, `ГЭСНр` отдельными typed потоками.
- [x] Никогда не выводить family/base_type из голого кода.
- [x] Ресурс наследует immutable parent norm id, а не повторно связывается по bare code.
- [x] Ключ нормы: edition + base_type + full code; provenance: файл/лист/строка/hash.
- [x] Конфликты, untyped rows, orphan resources и cross-family rows — в quarantine.
- [x] Генерировать `les_smeta_base_integrity_v1`; обязательные нулевые проверки:
  cross-family contamination, orphan resources, duplicate norm keys, resource-parent mismatch.
- [x] Сначала закрыть коллекции экспертных кейсов. `Нормы_распакованные` пока не трогать.

Срез typed v2 от 2026-07-10: приоритетные коллекции `08,15,17,63,67`; 3920 trusted-норм,
38168 ресурсов; `ГЭСН=1389`, `ГЭСНм=2153`, `ГЭСНп=157`, `ГЭСНр=221`; FGIS import errors=0.
Семейство `ГЭСНмр` поддержано схемой/importer, но в этом ограниченном срезе строк не получено.
Integrity: 0 cross-family, 0 orphan, 0 duplicate key, 0 parent mismatch, 0 missing provenance,
0 FTS coverage failures. 131 норм без полного имени/измерителя исключены, а не дополнены догадкой.

## Фаза 3 — norm browser и evidence

- [x] Exact lookup полного шифра.
- [x] Навигация family → collection → table → norm.
- [x] FTS/BM25 по названию и составу работ.
- [x] Exact normative identity и rehydrate полной записи остаются в typed SQLite.
- [x] Построить и подключить dedicated norm-card RAG: dense + native BM25 → RRF → BGE rerank →
  typed SQLite rehydrate. Общий пользовательский RAG и проектные датасеты не смешивать с нормами.
- [x] Карточка нормы: измеритель, состав работ, ресурсы, точный source ref.
- [x] Удалить case-specific boosts/anchors из legacy harness и direct-model prompts; оценивать
  универсальный typed browse без БАП/потолки/коробки-спецправил.

## Фаза 4 — деньги и форма ЛСР

- [x] Первый поддержанный регион/период: Санкт-Петербург, 2 квартал 2026.
- [x] Подозрительные aliases pricebook: byte-identical конфликт региона → quarantine.
- [x] Полный ФСЭМ machine → machinist: PDF 267 страниц → 1576 trusted машин; 978 строк с
  машинистами, 598 без машиниста; 6 конфликтных кодов quarantined. Агрегат ОТм заменяется
  детализацией только при полном покрытии и reconciliation часов.
- [x] НР/СП: убрать молчаливый fallback 100/49; unresolved остаётся gap строки.
- [x] НР/СП: 812/пр + 774/пр преобразованы в 135 provenance-rules для 108 коллекций;
  16 неоднозначных коллекций требуют явный `nr_sp_rule_id`, однозначные разрешаются автоматически.
- [x] Коэффициенты сначала ручные/выбранные моделью с источником; код только применяет.
- [x] КАЦ сначала явный ручной ввод; document workflow ≥3 КП остаётся следующим улучшением.
- [x] Сохранить текущую 12-графную РИМ-трассу и XLSX renderer; печатная форма зафиксирована как
  A4 landscape, одна страница по ширине (денежные графы не отделяются от наименований).
- [x] НДС для ценового периода 2026: 22% по versioned tax policy; в XLSX отдельно видны итог без
  НДС, НДС и итог с НДС. Непредвиденные затраты не добавляются без явного основания.
- [x] Primary ЛСР + детерминированный min/max по явно одобренным моделью/пользователем вариантам;
  код не добавляет варианты и не выбирает их применимость.

## Фаза 5 — живой цикл модели

- [x] Один универсальный цикл PDF ВОР → lossless `WorkItem` → iterative typed browse → явный выбор
  модели → revision → денежная ЛСР/XLSX. Модель может дать `retry_query`; код исполняет read-only browse.
- [x] Human correction — последующая immutable revision с `parent_revision_id`, не обязательный стоп
  перед первым денежным результатом.
- [x] Сметные chat/artifact/API consumers используют одно evidence/calculation ядро; raw calculation
  endpoints не заявляют workflow/evidence authority.

Универсальный intake ВОР из PDF создан: он сохраняет все видимые строки, разделы, единицы,
количества, примечания, координату и hash источника; норму, аналог и цену не угадывает.

## Приёмка

- [x] Главный acceptance-файл: `/Users/ovc/Documents/сметный модуль/для тестов/ВОР монтаж БАП П1 13.05.pdf`.
- [x] Из него без потерь прошли 4 раздела и 19 строк ВОР: 1 защитное укрытие, 1 демонтаж
  кабельной линии, 12 строк АР/отделки и 5 строк ЭОМ/БАП.
- [x] Результат по БАП — денежная ЛСР в форме Приложения №3 с каждой исходной строкой, полным
  norm/price trace и видимыми `0.00`/missing только там, где источник реально не закрыт.
- [x] Acceptance выполнен локально без платной облачной модели; облако не является условием
  работоспособности ядра.
- [x] 0 cross-family contamination в trusted machine base.
- [x] 100% входных строк ВОР представлены в ЛСР, включая missing/0.00.
- [x] Деньги и форма ЛСР получены в первом принятом полезном результате ручного модельного прохода.
- [x] Каждая сумма имеет trace цены/источника или явный missing.
- [x] Модель явно владеет norm/analog selection; код не содержит скрытого выбора. Для acceptance
  роль модели исполнил Codex, используя только model-visible инструменты LES.
- [x] Повторный расчёт детерминирован: нормализованный trace совпал, SHA-256
  `ef9b49a62800f659bc6b16af774f128aa40e40791da074813c249299238fefc9`.
- [ ] Экспертные кейсы: кровля, Столп, большая ЛСР 3096 строк, электрика, отделка, ОВ/ВК,
  missing prices, аналоги.

Новый zero-state срез БАП 2026-07-10: `priced_final`, 19/19 строк представлены; 16 строк
получили норму, 3 строки доказуемо покрыты комплексными позициями, `flags=[]`, `blockers=[]`.
271 571,73 руб. без НДС, НДС 22% 59 745,78 руб., 331 317,51 руб. с НДС. Источник заново
распознан по SHA `fc65ce15b5bb970a`; предыдущая ревизия не читалась, `parent_revision_id=""`.
Свежая полная Сплит-форма ФГИС: Санкт-Петербург, 2 квартал 2026, 281 223 строки. Три отсутствующих
товара (DKC 91920, PR08.2754, 40-0300-FR1.5-6) прошли КАЦ LES по 3 поставщикам с приведением
розничной цены к цене без НДС. Рабочий trace: `storage/lsr/bap_fresh_fgis_v2.json`; передаваемый
артефакт: `outputs/bap-fresh-fgis-v2/LSR_BAP_fresh_FGIS_2kv2026.xlsx`.

## Открытый долг после БАП

- [x] Исправить контракт аналога: если model decision называет выбор аналогом/указывает ограничения,
  `NormBinding.is_analog` обязан быть `true`, `analog_limitations` — непустым; иначе позиция не может
  получить `accepted` и попасть в денежный итог как обычная точная норма.
- [x] Ввести gate технологической применимости по карточке ресурсов до расчёта аналога. БАП-рецензия
  отклонила как минимум `ГЭСН15-01-054-01` для проёма в ГКЛ и `ГЭСНм08-03-545-05` для небольшой
  огнестойкой коробки; до нового выбора эти строки должны быть `MISSING`, а не priced.
- [x] Основные проектные материалы из ВОР должны иметь отдельные resource bindings и цену/`0.00`
  с причиной: люки, грунты, шпаклёвки, стеклохолст и клей, краски, БАП, кабель, гофра, скобы, коробки.
  Вспомогательные материалы нормы не считаются покрытием основного проектного материала.
- [x] Формальная ЛСР: заполнить стройку/объект/регион/ценовой период/номер/основание без локального
  пути; явно печатать `PRICED PARTIAL — НЕ ДЛЯ СОГЛАСОВАНИЯ` в шапке при blockers/ценовых gaps.
- [x] XLSX должен быть редактируемым расчётом: формулы по количеству, цене, стоимости, итогам и НДС,
  при сохранении immutable JSON trace как источника воспроизводимости.
- [x] Ввести model/user-owned resource decisions: замена нормативного материала, КАЦ, давальческий
  материал, повторное использование и исключение из стоимости с отдельным provenance.
- [x] Ввести coverage binding `work item -> другая позиция нормы`, чтобы одна комплексная норма могла
  доказуемо закрывать несколько строк ВОР без двойного труда и без ложного `MISSING`.
- [x] Удалить неоднозначный `quantity_multiplier`: код отдельно хранит source quantity, unit conversion
  и norm quantity; дополнительные слои требуют явной технологической операции/нормы, а не повторного
  умножения исходного объёма.
- [x] Перенести причины analog/MISSING из model decision trace в сам XLSX (примечание/лист проверки),
  а не оставлять их только в JSON/revision.
- [x] Добавить model-visible resource plausibility review: необычная техника (краны, подъёмники,
  сварка и т.п.) должна быть видна до подтверждения аналога с точной нормой, маш.-ч и стоимостью.
  Код не удаляет нормативный ресурс по «здравому смыслу»: модель меняет/подтверждает норму целиком.
- [x] Сжать evidence-пакет: массовый первичный hybrid/RRF возвращает компактные metadata-карточки,
  полная карточка читается через `read_norm`; rerank выполняется только для узкого повторного поиска.
- [x] Закрыть 5 строк БАП после реализации resource/coverage contracts: защитное укрытие,
  подготовка основания, демонтаж+повторный монтаж реечного потолка, отдельный ресурс скоб.

## Гейт после живой ЛСР

Актуальный live-аудит 2026-07-11 (GPT-5.4, каждый БАП с нуля): исходные 19 строк сохранены;
`8 шт → 8`, `3,2 м² → 0,032`, `160 м → 1,6`; кабель не использует норму АЭС; отсутствующие цены
и нормы остаются null/MISSING. Принятая аудиторская ревизия: 9 bound + 1 covered + 9 open,
86 442,11 руб. без НДС, НДС 22% 19 017,26 руб., всего 105 459,37 руб. Файл:
`outputs/bap-live/LSR_BAP_live_2026-07-11_FINAL.xlsx`. Норма 545-05 для коробки, полное устройство
реечного потолка и недоказанные шпатлёвки исключены из денег, а не замаскированы аналогом.

- [ ] Повторить штатный `/api/chat` после нового fail-closed technology contract и получить ту же
  честную структуру без аудиторской ревизии Codex.
- [ ] Пакетировать `read_norm` и ценовые tool calls одного хода; вынести model/embedding/retrieval/
  rerank/tool timings в API trace и UI progress.
- [ ] Дозагрузить в active v2 ГЭСНмр, технические части и приложения сборников; текущие gaps описаны
  в `docs/SMETA_REQUIRED_SOURCE_AUDIT_2026-07-11.md`.

- [x] Штатный чат получил общий zero-state путь `PDF attachment → source intake → model-owned
  norm/resource decisions → ФГИС/КАЦ → formula XLSX`; binary больше не теряется после read-upload,
  клиент не может передать произвольный filesystem path.
- [ ] Пройти этот путь на локальной Qwen через реальный `/api/chat/stream` с БАП и получить новый
  XLSX без prepared decision packet. Focused кодовый gate зелёный (`92 passed`), но первый live
  прогон обнаружил зависший MLX и после штатного restart — около 73 с на батч из трёх строк;
  shortlist/prompt уже сжаты, итоговый latency/result gate ещё не закрыт.

- [ ] Предметная проверка пользователем/живым сметчиком файла
  `outputs/bap-fresh-fgis-v2/LSR_BAP_fresh_FGIS_2kv2026.xlsx`.
- [ ] Подтвердить характеристики БАП. Сейчас видимо принято допущение: IP20, выходная мощность
  до 40 Вт, ФГИС `62.4.02.04-0008`; замена кода пересчитает материал без смены нормы монтажа.
- [ ] Подтвердить допустимость аналогов: защитное укрытие по норме оклейки рулонным материалом,
  отверстия ГКЛ по монтажным отверстиям натяжного потолка, скрытый люк по общему люку 400х400,
  монтаж огнестойкой коробки по норме небольшого электрического аппарата.
- [ ] Получить формальные КП/оферты для КАЦ DKC 91920, PR08.2754 и 40-0300-FR1.5-6. Текущий
  денежный результат использует доступные веб-цены с URL и явно принятым НДС 22%.
- [ ] После предметного принятия: точечные тесты нового ядра → `make verify` → `make test` →
  актуализация MODULE_INDEX/CODE_MAP/SMETA_MECHANICS/skill/version/RELEASE_LEDGER → `make ship` → push.

## Что сознательно срезано

- Только СПб 2 кв. 2026 на первом проходе.
- Ручные КАЦ и коэффициенты.
- Без 85 регионов, GraphRAG, multi-agent и переписывания UI.
- Без полного импорта `Нормы_распакованные`, пока typed importer и integrity gate не готовы.

## Отложенный долг общего LES — после живой сметы

- [ ] Унифицировать модули на общее retrieval-ядро с параллельными exact/typed и hybrid-каналами:
  query normalization → dense+BM25 → RRF → filters → BGE rerank → parent/evidence rehydrate.
- [ ] Закончить полный чистый contract-index общего корпуса с одним embedding fingerprint; текущий
  bounded canary не переключать в production до полного покрытия и аудита.
- [ ] Убрать module-local SQLite/FTS-only поисковые обходы там, где нужен смысловой retrieval;
  SQLite/graph/table оставить источниками истины и точного rehydrate, а не единственным поиском.
- [ ] Для text/PDF оставить parent/context window; для typed-сущностей восстанавливать полную запись,
  связи и provenance из их доверенной базы.
# 0.24.0.381 — снятие ограничений модели

- [x] Удалить обязательные resource-review, dominant-review и impact-repair из document workflow.
- [x] Передавать ресурсные действия модели непосредственно через `bind_norm`.
- [x] Убрать фиксированные лимиты запросов, карточек и страниц; оставить выбор размера выдачи модели.
- [x] Заменить длинную runtime-методичку коротким smeta skill для tool-loop.
- [ ] Повторить БАП с нуля через обычный чат и сохранить XLSX как приёмочный результат 0.24.0.381.
