# Session Summary 12 — ЛЕС как мозговой центр проекта (брейн W17) + визуалайзер + устойчивость

Workspace: `/Users/ovc/Projects/LES_v2` · Runtime clone: `/Users/ovc/Projects/LES_v2_reinstall_stress`
Дата закрытия: `2026-06-14` · Ветка: `feat/les3-p1` (синхронна с origin, HEAD `444d058`)
Предыдущий: `SESSION_SUMMARY_11.md`. `make test`: **685 passed, 0 failed**.

---

## 🔜 ПРОМТ-ХЕНДОФФ (вставить в следующую сессию)

> Продолжаем LES3 по `docs/LES3_PLAN.md`. Прочитай `AGENTS.md`, `docs/CODE_MAP.md`, `SKILL.md`, память,
> и `docs/PROJECT_KNOWLEDGE_GRAPH.md` (архитектура «второго мозга»). Принципы: **LLM-минимализм** (ADR-11),
> **облако только OpenRouter+OpenAI** (ADR-9), **GUI-first**, тон ответов — слегка ироничный (вшит в sys_normal).
> После шага: доки+SKILL+CODE_MAP+план, `make verify`, commit+push, деплой в рантайм-клон
> (`git pull /Users/ovc/Projects/LES_v2 feat/les3-p1` + `launchctl kickstart me.ovc.les.proxy/com.les.sovushka`).
> Чат-проверки требуют LLM — временно `llm_provider:mlx`, потом вернуть openrouter. SSH иногда отваливается —
> деплой локальным pull, пуш догнать позже.
>
> **Волна 17 (мозговой центр проекта)** — порядок брейна **W17.5✅ → W17.3 → W17.4 → W17.6 → W17.7 → W17.8**.
> Готово: W17.1 (объект+двойной режим), W17.2 (типизированные рёбра), W17.5 (КАРТА ОБЪЕКТА). **Бери W17.3**
> (доменная онтология: классификационный хребет Element→System→Space→Floor + состояния CDE WIP/Shared/
> Published/Archived). Используй СП 48 (организация строительства — он в индексе) для сущностей процесса.
> Дальше W17.4 (граф проекта — наложить личный слой+рёбра на W5.7), W17.6, W17.7 (норм→элемент), W17.8 (LLM-синтез, gated).
>
> Открытые вопросы оператора: **ЗПН** — не федеральная аббревиатура, уточнить (для Волны 19); **ГрК** в индексе
> базовая ред.2005 — заменить на актуальную; партиционирование задач/объёмов/заметок по объекту (сейчас в досье глобальны).
> Параллельные волны: **W18** (визуалайзер — остаток W18.2-18.5), **W19** (генерация документов РФ — формы+XML Минстроя).

---

## Что сделано в этой сессии

### Брейн — Волна 17 «ЛЕС как мозговой центр проекта»
- **Архитектура** (`docs/PROJECT_KNOWLEDGE_GRAPH.md`, v2) на основе исследования (BASB/PARA/Zettelkasten/MOC +
  GraphRAG/онтология + ISO 19650/Uniclass/decision-logs): берём ФОРМУ графа знаний, но инвертируем стоимость —
  связи детерминированно, LLM last-resort; provenance+confidence; project_id default-on; нормативы scope=global.
- **W17.1** объект+двойной режим: `project_service` (`les_projects`/`les_project_links`, `project_dataset_ids`);
  `/api/projects/*`; `chat.py` сужает ретрив к датасетам объекта (project_id), иначе обычный RAG; GUI-селектор.
  Проверено: project_id=1 → источники только СПДС. 7 тестов.
- **W17.2** типизированные рёбра: `edge_service` (`les_edges`, экстракторы НТД/`[[вики]]`/Source ID,
  `derive_edges_from_text` идемпотентно, bom write-time в create_note/create_task, `/api/edges` + backfill).
  Проверено: заметка → 4 ребра. 10 тестов.
- **W17.5** КАРТА ОБЪЕКТА: `build_dossier` + `/api/projects/{id}/dossier` (нормативы/задачи/объёмы/связи/BIM/
  PARA, 0 LLM); GUI-диалог у селектора объекта. Проверено: ЖК Сосновый → NTD_SPDS 77ф/7540ч, связей 5. 9 тестов.

### Чат / визуалайзер
- **W18.1** файл-вьювер: `files.py` (`/api/rag/tree`,`/file/text`,`/file/raw`, path-guard, 7 тестов); фронт
  drawer «Файлы» (нативный `ui.tree` + codemirror/картинки/PDF-iframe). Проверено вживую.
- **W18.6** цитаты из источников: `source_excerpts(chunks)` в ответ; раскрываемая секция «Цитаты из источников»
  под ответом (фрагмент СП/ГОСТ + score + «открыть»→W18.1). Проверено: 6 цитат с текстом. 5 тестов.
- Панель «Задачи и объёмы» в чат-шелле (IA-решение оператора) + фикс «мёртвого» UI (add_log не роняет сборку).

### Устойчивость / провайдеры
- **Облачный фолбэк по цепочке моделей** + таймаут на модель (`LES_CLOUD_MODEL_TIMEOUT_SEC=45`): зависла →
  следующая → MLX. `OPENROUTER_MODELS` через `/api/settings`. Оператор задал цепочку (mimo/v4-pro/nemotron/minimax). 7 тестов.
- **Агент-ревью** сессии (3 агента) → исправлены: NameError `llm_url`, индикатор-offline при долгом SSE,
  кластер «зависший push→фриз UI» (read-timeout+heartbeat), await отмены runner. Bridge/Speckle/W17.1 — чисто.
- **cloud-consent тумблер** в ⚙ (P2→облако); per-dataset P0/P1/P2 уже был в САМОВАРе.

### Данные / планы
- **ГрК РФ** (оператор дал PDF) — зарегистрирован в GKRF (**PENDING**, ждёт окно памяти; ред.2005 — заменить).
  **СП 48** «организация строительства» — уже был в индексе.
- **Волна 18** (визуалайзер) и **Волна 19** (генерация документов РФ: АОСР/ЗОС/РНС/ТУ/ПЗ + реестр XSD Минстроя) —
  планы на основе исследований. WCAG-правки (фокус/lang/reduced-motion/aria) — из прошлой сессии.

## Состояние
- Origin синхронен (`444d058`); рантайм-клон обновлён, proxy+sovushka живы. `make test` 685/0.
- Активный провайдер — openrouter (для НТД/P0 всё равно MLX по W3.3). Тестовый объект «ЖК Сосновый (тест W17.1)»
  в рантайме (id=1, привязан NTD_SPDS) — можно оставить/снести.

## Принципы/решения
- Граф знаний: форма GraphRAG + инвертированная стоимость (детерминированно, LLM last-resort); provenance+confidence.
- Тон ответов: слегка ироничный (sys_normal), факты строгие; sys_strict без улыбки.
- Цитаты: показываем конкретный фрагмент нормы, не только имя файла (запрос оператора).
