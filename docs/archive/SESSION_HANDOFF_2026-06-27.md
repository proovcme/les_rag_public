# Хендофф сессии — 2026-06-27

Ветка `feat/les3-p1` (= `main`). Все пункты задеплоены на рантайм `/Users/ovc/LES` и проверены вживую.

## Что сделано (по порядку)

| Тема | Коммит | Статус |
|---|---|---|
| **БЕЗОПАСНОСТЬ: les.ovc.me был открыт чатом публике без пароля** | `07aeb7d` | закрыто, verified |
| Нормоконтроль **Phase 4 — API** `/api/doc-review/{rulepacks,run,download}` | `8de4909` | live на 674-файл. комплекте |
| Нормоконтроль **Phase 4 — UI** «Проверка документации» в Инструментах | `6260e14` | (вкладка отцеплена — см. долг) |
| Чат-раскладка «не лезла в экран» (QScrollArea оверфлоу) | `e1556b7` | verified |
| Чат-guard ложно блокировал (залипший своп macOS) | `.env` рантайма | verified flip |
| Смета **#2** — рендер трассы в XLSX | `f5e92ac`→`530f07b` | **форма Приложения 4 (эталон)**, live |
| Смета **#3** — справочник ФСЭМ «машина→машинист» в yaml | `b148f27` | эталон 11813.04 цел |
| Смета **#4** — golden на обезличенной GRAND-позиции | `82e85be` | 118.8М, все поля точно |
| **doc-review через ЧАТ** (агент-роутер, не GUI-вкладка) | `aa11de4` | live: cache=doc_review |
| **Парсинг датасетов** — видимый статус+лог (доверие к «плей») | `239d228` | verified (PARSING-индикатор) |
| **Codex-режим-чипы** в чате (Поиск/Сметы/Нормоконтроль) | `0bb5c6d` | live: чип→mode=doc_review |
| Нормоконтроль **Phase 5** — title_block_extract (штамп) → D4 computed | `96e164b` | live: D4-002 supported |

**Нормоконтроль (Phase 1→5) — рабочая вертикаль:** RAG-led, доступен из чата (режим-чип / «проверь
комплект по ГОСТ») и из API; статусы честные (код считает формализуемое, сканы → ручная/OCR, вердикт за
инженером). **Смета РИМ:** трасса позиции → форма Приложения 4 (эталон ГРАНД), числа проверяемы.

## Долг (что НЕ закрыто)

1. **Смета: multi-position ЛСР.** Рендер Приложения 4 делает ОДНУ позицию; эталон — многопозиционный
   (разделы, позиции 1..N, общий итог). Нужно: `la.assemble` (многопозиционный) → renderer + группировка
   по разделам + свод по ЛСР. Файл `proxy/services/rim_trace_xlsx_service.py`.
2. **Смета: полная GRAND-фиделити.** Эталон = 179 внутренних колонок; НР/СП с типом работ («НР Кровли»),
   полная разбивка сметной стоимости (строит/монтаж/оборуд/прочие). Текущий рендер — верный СТАРТ, не
   pixel-perfect. Метаданные шапки (стройка/объект/субъект/уровень цен) сейчас дефолтные → провести
   реальные из проекта/LES.md.
3. **title_block_extract: OCR для сканов.** Сканы (нет текст-слоя) → честный manual/OCR. Подключить
   Tesseract (см. [[local-ocr-engine]]) → штамп и на сканах → полное computed-покрытие D4-002.
4. **doc-review: retrieval-подфаза.** Цели `kind: retrieval` (D2-001 и пр.) сейчас `review_needed` (не
   выполнены) — нужен RAG-поиск текста требования ГОСТ + цитата. `doc_review_service.run_review`.
5. **GUI-вкладка «Инструменты» отцеплена** (чистка v0.24): `build_instrumenty()` (ВОР/нормоконтроль/дифф/
   doc-review-UI) НЕ подключена. Доступ к нормоконтролю восстановлен ЧЕРЕЗ ЧАТ (режим-чип) — оператор не
   просил возвращать вкладку. Если понадобится — `sovushka_ng.py` + `header.py` (добавить tab_tools).
6. **Мелочь:** `lsr.py:129` docstring `/rim-trace/export` ещё упоминает «Источник» (старый рендер) —
   косметика, на поведение не влияет.

## Задачи на следующую сессию (приоритет)

1. **Multi-position Приложение 4** — собрать многопозиционный ЛСР в форму эталона (разделы + общий итог).
   Самый крупный смета-шаг; данные (`la.assemble`) есть.
2. **title_block OCR** — Tesseract для сканов → D4-002 полное покрытие.
3. **doc-review retrieval-подфаза** — RAG находит текст требования ГОСТ (цитата) для `kind: retrieval`.
4. **Метаданные шапки Приложения 4** — реальные стройка/объект/субъект/уровень цен из проекта.
5. **Полировка GRAND-фиделити** — НР/СП с типом работ, разбивка сметной стоимости.

## Состояние рантайма (durable, в `.env` — не в гите)

- **TRUSTED_PROXY_NETWORKS** включает VPS-Caddy `10.195.146.136/32` — иначе публичная дыра. [[les-ovc-me-topology]]
- **Чат-guard** под 24GB M4: `LES_CHAT_MIN_FREE_GB=6`, `LES_CHAT_MAX_SWAP_USED_GB=12`, `LES_CHAT_SWAP_RELIEF_FREE_GB=5`. [[chat-guard-macos-stale-swap]]
- **Профиль эмбеддинга**: `LES_EMBED_PROFILE=qwen` (коллекция `les_rag_qwen3_06b`). [[rag-index-completeness-and-ops]]
- **Агент-роутер ОСНОВНОЙ** (`LES_ROUTER_PRIMARY=true`) — понимание свободного текста делает LLM, не keyword-гейты.
- **Режим-чипы чата**: Поиск=`rag` · Сметы=`smeta` · Нормоконтроль=`mode=doc_review` (→ RAG-led нормоконтроль).

Проверки: `curl -D- https://les.ovc.me/classic` → 307/login · `POST /api/doc-review/{ds}/run` ·
`POST /api/lsr/rim-trace/export` → форма Приложения 4 · режим-чип «Нормоконтроль» в чате.
