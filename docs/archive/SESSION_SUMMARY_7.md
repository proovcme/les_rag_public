# Session Summary 7 — LES3: рабочая память, карта архива, инструменты в GUI

Workspace: `/Users/ovc/Projects/LES_v2` · Runtime clone: `/Users/ovc/Projects/LES_v2_reinstall_stress`
Дата закрытия: `2026-06-14` · Ветка: `feat/les3-p1` (синхронна с origin)

---

## 🔜 ПРОМТ-ХЕНДОФФ (вставить в следующую сессию)

> Продолжаем LES3 по `docs/LES3_PLAN.md`. Прочитай `AGENTS.md`, `docs/CODE_MAP.md`, `SKILL.md`
> и память. Принципы: **LLM-минимализм** (ADR-11), **облако только OpenRouter+OpenAI**,
> **GUI-first** — любая функция обязана иметь кнопку в Совушке, не только API.
> После каждого шага: обновляй документацию + SKILL + CODE_MAP + план, `make verify`, git commit+push,
> деплой в рантайм-клон (`git pull /Users/ovc/Projects/LES_v2 feat/les3-p1` + `launchctl kickstart`).
> Если GitHub SSH таймаутит — деплой идёт через локальный pull, пуш догнать позже.
>
> Бери по порядку, что не упирается в сеть:
> 1. **W6.7** — вьювер↔чат двусторонняя подсветка: CAD/BIM-чанки выдачи несут `source_id`,
>    proxy кладёт `source_ids` в ответ чата/retrieve-debug, АТЛАС-вьювер слушает и подсвечивает
>    элементы (без ручного выбора). Приёмка: вопрос «где латунные фитинги?» → подсветка в АТЛАСе.
> 2. **W5.6-остаток** — фильтры clarification вместе с оператором (уточняющие вопросы → выбор датасета).
> 3. **W11.2-остаток** — план/факт (ВОР ↔ журнал полевых объёмов); зависит от W8.1 (журнал объёмов).
>
> ⚠️ Сначала проверь: докачался ли **bge-reranker-v2-m3** (`du -sh ~/.cache/huggingface/hub/models--BAAI--bge-reranker-v2-m3`,
> цель ~2.3 ГБ). Если да — закрой задачу #2 (W2.2/W2.3): замер латентности `/v1/rerank`,
> domain gate 16/16 через rerank-путь, 3 живых кейса (fire_truck_access, ширина эвакуации, разделы ПД-87).

---

## Что сделано в этой сессии

### Волна 16 — Рабочая память («шаг к Cursor») — ЗАКРЫТА
- **W16.2 Задачник** — `proxy/services/task_service.py` (таблица `les_tasks`), `/api/tasks` CRUD,
  чат-команды ДО admission (работают при memory-guard): «поставь задачу …» / «что по задачам?» /
  «задача N готова» — regex+SQL, 0 LLM. 8 тестов.
- **W16.3 Заметки** + **W16.1 Recall** — `proxy/services/memory_service.py`: «запомни: …» /
  «заметки» / «забудь заметку N»; лексический recall (стем-пересечение, без эмбеддингов) подмешивает
  релевантные заметки и прошлые VERIFIED-ответы в промпт И в validation-контекст. Recall считается
  ДО clarification (проектные вопросы вроде «корпус Б» иначе режутся уточнением). 7 тестов.
- **UI**: вкладка **ЗАДАЧИ** (`sovushka/pages/zadachi.py`) — задачник + заметки, + `/api/notes`.

### W15.2 — Выборочная индексация из карты — ЗАКРЫТА
- `resolve_selection`/`suggest_index_candidates` в `file_map_service.py` + `POST /api/filemap/index`
  (создаёт/дополняет датасет, фильтр prefix/cipher/ext/rel_paths, втягивает через `backend.upload_file` —
  staging-копию делает система, не оператор) + `GET /api/filemap/candidates`.
- **UI**: блок «КАРТА АРХИВА» в С.А.М.О.В.А.Р. (скан + папки-кандидаты с кнопкой ИНДЕКС). 3 теста.
- E2E на рантайме прошёл: скан → ИНДЕКС ветки → вопрос в чате → VERIFIED со ссылкой на файл.

### W11.2 — Детерминированные инструменты в GUI (GUI-first) — ЗАКРЫТА (остаток: план/факт)
- Вкладка **ИНСТРУМЕНТЫ** (`sovushka/pages/instrumenty.py`): ВОР (сформировать/предпросмотр/скачать xlsx),
  нормоконтроль (проверить/отчёт), дифф моделей и текста. Раньше всё это было только через curl.
- `GET /api/diff/cad-bim/imports` (список ревизий для выбора), `api_get_bytes` (скачивание xlsx в браузер).

## Runtime state (на момент закрытия)
- Proxy `:8050` health `ok`, Sovushka `:8051` `/les/classic` 200, MLX `:8080`, Qdrant `:6333`.
- Метабаза `data/les_meta_qwen.db`: добавлены таблицы `les_tasks`, `les_notes`.
- CAD/BIM граф: 1 импорт (тестовый плагин оператора, 399 элементов).
- Деплой-флоу: commit+push в LES_v2 → `git pull` в рантайм-клоне → `launchctl kickstart`.

## Блокеры / незавершённое
- **bge-reranker-v2-m3** качается в фоне (pid менялся; ~17 МБ из 2.3 ГБ) — внешние CDN сегодня
  душат канал (~9 КБ/с, зеркало хуже). W2.2/W2.3 ждут модель = задача #2 в задачнике ЛЕС.
- GitHub SSH периодически таймаутит — деплой через локальный pull надёжен, пуш догоняется.
- `[live]`-приёмки ждут оператора: скан реального архива (W15.1/15.2), ВОР/дифф/нормоконтроль
  на реальных данных, дифф двух реальных ревизий.

## Принципы, зафиксированные в память
- LLM-минимализм (ADR-11), облако только OpenRouter+OpenAI (ADR-9), **GUI-first** (новое 2026-06-14).
